# QR 基础知识（Quant Research Foundations）

> **定位**：这是 AlphaStream 配套的**数学 / 统计基础知识库**，给"想搞懂公式背后到底是什么"的你。
> 配套文档：
> - `QUANT_RESEARCH_GUIDE.md`：术语速查 + 九阶段流程（讲"有什么词"）
> - `COLLABORATION_PLAN.md`：Zeon×Kani 协作方案（X 契约、风险模型怎么交接）
>
> 本文档**不重复术语**，只讲基础知识的「直觉 + 公式 + 为什么量化要用它」。
> 公式用 LaTeX（行内 `$...$`、块 `$$...$$`），GitHub / VS Code 均可渲染。

---

## 1. 线性代数：矩阵是量化的语言

量化里几乎所有东西都是矩阵：股票收益是一列向量，因子暴露是一张矩阵，风险是一张协方差矩阵。

### 1.1 向量与矩阵
- **向量**：一列数。如某天的 N 只股票收益 $r = (r_1, r_2, \dots, r_N)^\top$，是 $N\times 1$ 列向量。
- **矩阵**：多列拼起来。如因子暴露矩阵 $B$ 是 $N\times K$（N 只股票 × K 个因子）。

### 1.2 转置（transpose）
把行变列、列变行，记 $A^\top$。$(N\times K)$ 转置后是 $(K\times N)$。
- 直觉：转置常用来"对齐维度"做乘法。

### 1.3 矩阵乘法
$C = A \cdot B$，要求 **A 的列数 = B 的行数**。结果 $C$ 的行数 = A 的行数，列数 = B 的列数。
- 元素定义：$C_{ij} = \sum_k A_{ik} B_{kj}$（A 的第 i 行 点乘 B 的第 j 列）。
- **维度规则是量化里最容易出错的地方**：比如 $\Sigma = B V B^\top$ 能算，前提是 $B$ 是 $N\times K$、$V$ 是 $K\times K$、$B^\top$ 是 $K\times N$ → 结果 $N\times N$。维度对齐了才合法。

### 1.4 对角矩阵 / 单位矩阵
- **对角矩阵**：只有主对角线非零，如特异方差 $\Delta = \text{diag}(\delta_1,\dots,\delta_N)$。
- **单位矩阵 $I$**：对角线全 1，乘任何矩阵不变。

---

## 2. 单变量统计：均值、方差、标准差

### 2.1 均值（mean）
$$\bar x = \frac{1}{T}\sum_{t=1}^{T} x_t$$
直觉：这组数据的"中心"或"平均水平"。

### 2.2 方差与标准差（variance / std）
$$\text{Var}(x) = \frac{1}{T-1}\sum_{t=1}^{T}(x_t - \bar x)^2, \qquad \sigma = \sqrt{\text{Var}(x)}$$
- 直觉：**离散程度**。收益波动越大，方差 / 标准差越大，风险越高。
- 分母用 $T-1$（样本方差）是为了**无偏估计**（自由度校正：估均值已用掉 1 个自由度）。
- 标准差和原数据同单位，比方差更好解释。

---

## 3. 协方差与相关性（多变量一起看）

### 3.1 协方差 Cov(X, Y)
$$\text{Cov}(X,Y) = \frac{1}{T-1}\sum_{t=1}^{T}(X_t-\bar X)(Y_t-\bar Y)$$
- 直觉：**两个变量一起变动的方向与幅度**。
  - 正：X 高时 Y 也倾向高（同涨同跌）
  - 负：X 高时 Y 倾向低（对冲）
  - 接近 0：没有线性关系
- 特例：$\text{Cov}(X,X) = \text{Var}(X)$，所以**方差是协方差的特例**。

### 3.2 相关系数 ρ（correlation）
$$\rho_{XY} = \frac{\text{Cov}(X,Y)}{\sigma_X\,\sigma_Y}$$
- 把协方差除以两边标准差，**归一化到 $[-1, 1]$**。
- 为什么需要它：协方差大小受量纲影响（价格高的股票协方差天然大），ρ 去掉量纲后可直接比"相关性强弱"。
- 因子构建里要求因子间低相关（|ρ| 小），就是为了避免冗余、防止因子暴露矩阵 $B$ 病态。

### 3.3 协方差矩阵 Σ（核心）

把一组变量的**两两协方差**排成一张方阵：

$$
\Sigma =
\begin{bmatrix}
\text{Var}(r_1)   & \text{Cov}(r_1,r_2) & \cdots & \text{Cov}(r_1,r_N) \\
\text{Cov}(r_2,r_1) & \text{Var}(r_2)   & \cdots & \text{Cov}(r_2,r_N) \\
\vdots            & \vdots            & \ddots & \vdots            \\
\text{Cov}(r_N,r_1) & \text{Cov}(r_N,r_2) & \cdots & \text{Var}(r_N)
\end{bmatrix}
$$

性质：
- **维度 $N\times N$**，N 只股票。
- **对称**：$\text{Cov}(r_i,r_j)=\text{Cov}(r_j,r_i)$，所以 $\Sigma^\top = \Sigma$。
- **半正定（positive semi-definite）**：对任意权重向量 $w$，有 $w^\top \Sigma w \ge 0$（方差不可能为负）。
- **对角线 = 每只股票自己的方差；非对角线 = 两两协方差**。

**⚠️ 协方差的自变量是什么**：矩阵里的 $r_i, r_j$ 都是**收益率时间序列**（随时间的随机收益），协方差是在**时间维度 $T$** 上算的——即同一段历史里，资产 i 和 j 的收益"怎么一起动"。所以若写成 $\text{Cov}(X, Y)$，X、Y 就是两条资产（或因子）的**收益序列**；它们绝不是价格、市值或 PE。实务第一步永远是先从价格算收益：$r_t = P_t/P_{t-1} - 1$（或对数收益）。

**为什么量化离不开 Σ**：
组合收益 $R_p = w^\top r = \sum_i w_i r_i$，其方差（即组合风险）为：
$$\text{Var}(R_p) = w^\top \Sigma\, w$$
展开就是"各股票方差的加权 + 两两协方差的加权"。**分散投资能降风险，本质就是靠非对角线上的（负 / 低）协方差**。这正是最小方差组合 $\min w^\top\Sigma w$ 要优化的对象——也是 AlphaStream 优化的核心。

---

## 4. 回归基础：alpha 与 beta 从哪来

### 4.1 简单线性回归
$$y = \alpha + \beta x + \varepsilon$$
- $y$：被解释变量（如个股收益）
- $x$：解释变量（如市场收益 / 某个因子）
- $\alpha$：截距（intercept）
- $\beta$：斜率（slope），即"x 每动 1 单位，y 动多少"
- $\varepsilon$：残差（解释不了的部分）

### 4.2 OLS 怎么估 β 和 α
最小二乘法让残差平方和最小，可得闭式解：
$$\hat\beta = \frac{\text{Cov}(x,y)}{\text{Var}(x)}, \qquad \hat\alpha = \bar y - \hat\beta\,\bar x$$
- **β 就是"协方差除以方差"**——把第 3 节的知识直接用上了。
- 在多因子模型里，$r = B f + \varepsilon$ 本质就是对每个时点做"收益对因子暴露"的回归，$f$ 就是回归系数（因子收益）。
- 这和我们之前讲的"beta = 斜率、alpha = 截距、ε = 残差"的回归图完全对应；更完整的 $f$ 估计见 §6.3。

---

## 5. 标准化：z-score

$$z = \frac{x - \mu}{\sigma}$$
- 把任意量纲的变量变成**均值 0、标准差 1**。
- 在因子构建里：每个因子的原始值（如市值对数、过去收益）量纲不同，必须 z-score 标准化后才能并排放进因子暴露矩阵 $B$ 的同一列比较。
- 这也是为什么 $B$ 里的元素是无量纲的暴露度。

---

## 6. 把前面串起来：多因子模型里的矩阵

### 6.1 收益模型
$$r = B\cdot f + \varepsilon$$
- $r$：$N\times 1$ 股票收益
- $B$：$N\times K$ 因子暴露矩阵（每行一只股票，每列一个因子）
- $f$：$K\times 1$ 因子收益
- $\varepsilon$：$N\times 1$ 特异收益

### 6.2 风险模型（协方差分解）
$$\Sigma = B\cdot V\cdot B^\top + \Delta$$
- $V = \text{Cov}(f)$：$K\times K$ **因子协方差矩阵**（因子之间怎么一起动）
- $\Delta = \text{diag}(\text{Var}(\varepsilon))$：$N\times N$ **特异方差对角矩阵**（个股自己独有的风险，不可被因子解释）
- 推导直觉：$r$ 的方差 = $\text{Cov}(Bf+\varepsilon) = B\,\text{Cov}(f)\,B^\top + \text{Cov}(\varepsilon) = B V B^\top + \Delta$（假设因子与特异收益不相关）。

**这张图就是整个风险模型的骨架**：不直接估 $N\times N$ 的 $\Sigma$（样本不够、噪声大），而是用 $N\times K$ 的 $B$ + $K\times K$ 的 $V$ + 对角 $\Delta$ 拼出来，参数规模从 $O(N^2)$ 降到 $O(NK+K^2)$，既稳又可解释。AlphaStream / Barra 的风险模型都长这副样子。

**⚠️ 因子模型里其实有"三个"协方差矩阵（别混）**

| 矩阵 | 维度 | Cov 的 X、Y 是什么 | 怎么来的 |
|---|---|---|---|
| **Σ**（资产协方差） | $N\times N$ | 资产 i、j 的**收益** $r$ | 由下面两个合成 |
| **F / V**（因子协方差） | $K\times K$ | 因子 a、b 的**收益** $f$ | $F=\text{Cov}(f)$，需 Ledoit-Wolf 收缩（§10） |
| **D / Δ**（特异方差） | $N\times N$ 对角 | 个股**残差** $\varepsilon$ | $D=\text{Cov}(\varepsilon)$ |

合成关系就是上面的 $\Sigma = X\cdot F\cdot X^\top + D$。注意：**X（暴露矩阵）是结构输入，F 和 D 才是真正的协方差，它们的自变量分别是因子收益 $f$ 和残差 $\varepsilon$。**

**记号澄清（关键）**：你项目 `COLLABORATION_PLAN.md` 里的 **X 是因子暴露矩阵**（= 这里一直在造的 $B$，N×K），它**不是** $\text{Cov}(X,Y)$ 里的那个变量。协方差的自变量永远是**收益**——F 和 D 都得先有收益 $r$，才能从 $r = X f + \varepsilon$（§6.3 横截面回归）估出 $f,\varepsilon$ 后再算。这也正是评审 plan 时标红的坑：「个股收益 $r$ 未定义交接」——Kani 那段直接 `LedoitWolf(X)` 落在暴露上，跳过了"先有 $r$ 回归出 $f$、$\varepsilon$ 再收缩"这步。

### 6.3 因子收益 $f$ 怎么估：横截面回归（每个时点 $t$）

把 §6.1 的向量"钉"在**某一个时点 $t$**：
- **$r_t$**：时点 $t$ 所有 N 只股票的收益率，$N\times 1$ 向量（同一天、不同股票 = **横截面**）
- **$B_t$**：时点 $t$ 的因子暴露矩阵，$N\times K$（已在因子构建阶段造好，即你的 $X$）
- **$f_t$**：K 个因子在时点 $t$ 的"收益"，$K\times 1$ —— 这次回归要估的**系数**
- **$\varepsilon_t$**：个股特异收益，$N\times 1$

对 $r_t = B_t f_t + \varepsilon_t$ 做 OLS（横截面回归），闭式解即多元回归：
$$f_t = (B_t^\top B_t)^{-1} B_t^\top r_t$$
（这正是 §4.2 单变量 $\hat\beta=\text{Cov}(x,y)/\text{Var}(x)$ 的矩阵推广。）

对每个时点 $t$ 都算一次 → 得到 $\{f_1,\dots,f_T\}$，即 **K 个因子的收益时间序列**（T×K）。由此再回到 §6.2：
- **$V = \text{Cov}(f)$**（$K\times K$，实战要 Ledoit-Wolf 收缩）→ 即风险模型里的 $F$
- **$\Delta = \text{Cov}(\varepsilon)$**（$N\times N$ 对角，特异方差）→ 即 $D$
- 最终 $\Sigma = B\,V\,B^\top + \Delta$

**⚠️ 和 CAPM 的 $\beta$ 是"反方向"的（最容易混）**
- **CAPM**：**时序回归**——一只股票，跨时间，收益对市场收益回归 → 得到该股票的 $\beta_i$（它的敏感度，即 $B$ 的一行）。
- **多因子**：**横截面回归**——一个时点，跨股票，收益对暴露回归 → 得到当天的**因子收益 $f_t$**（$V$ 一列的来源）。
两者都是 OLS，只是"自变量是谁、跨哪个维度"不同。

**衔接 AlphaStream / COLLABORATION_PLAN**
- $f$ 必须先有**个股日收益 $r$** 才能算。所以 plan 评审里"个股收益 $r$ 未定义交接"是底层缺口：Kani 原代码直接 `LedoitWolf(X)` 在**暴露**（无量纲 z-score）上，等于跳过了"用 $r$ 回归出 $f/\varepsilon$"这步。Barra 正确做法是先有 $r$ → 横截面回归出 $f$、$\varepsilon$ → 再收缩得 $V$、$\Delta$。
- 这也说明 `Data/` 除 `hk_market_raw`（价格/市值/PE/量）外，还需要一份**个股日收益 $r$** 喂给风险模型。

---

## 7. 常用补充基础

- **相关 ≠ 因果**：ρ 高只说明一起动，不代表谁导致谁。因子研究要小心"伪相关"（两个因子都受第三变量驱动）。
- **正态分布假设**：很多风险模型假设收益近似正态，此时方差 / 协方差就能完整描述分布；但真实收益有肥尾，需压力测试 / 情景分析补刀。
- **横截面 vs 时序**（数学视角）：
  - 横截面：同一时点、跨股票（如某天所有股票的因子暴露 = $B$ 的一行，或某天所有股票收益对暴露的回归）→ z-score、因子构建、以及**因子收益 $f$ 的估计**多在此。
  - 时序：同一股票、跨时间（如某股票收益对**已知因子收益 $f$** 的回归）→ 估计的是该股票对因子的暴露 $B$（即 β）。注意：$f$ 是时序回归的"输入"而非"输出"；输出 $f$ 的是横截面回归（见 §6.3）。

---

## 8. 金融绩效指标：怎么衡量一个因子 / 策略"好不好"

因子和组合最终都要用数字评判。下面一组是回测与因子验证里最高频的指标。每个指标都会拆成：公式是什么、每个变量是什么、某个变量增大/减小会让指标增大/减小、以及背后的直觉。

（alpha / beta 已在 §4 讲过；波动率即收益的标准差，已在 §2 讲过。）

---

### 8.1 夏普比率 Sharpe Ratio

$$\text{Sharpe} = \frac{\bar R_p - r_f}{\sigma_p}$$

**变量分别是什么**
- $\bar R_p$：组合在一段时间内的**平均收益率**（比如年化 15%）。
- $r_f$：无风险利率（比如国债年化 3%）。
- $\bar R_p - r_f$：组合的**超额收益**（12%）。
- $\sigma_p$：组合收益率的**标准差**（总波动，比如 10%）。

**变量增大/减小 → 指标怎么变 → 为什么**
- $\bar R_p$ **增大** → Sharpe **增大**。
  - 为什么：分子变大，分母不变，分数整体变大。
- $r_f$ **增大** → Sharpe **减小**。
  - 为什么：分子 $(\bar R_p - r_f)$ 被压缩。
- $\sigma_p$ **增大** → Sharpe **减小**。
  - 为什么：分母变大，意味着每单位风险换到的收益变少。波动是“坏东西”，只耗损不创造价值。
- $\sigma_p$ **减小** → Sharpe **增大**。
  - 为什么：同样的收益，波动越小，风险调整后收益越高。

**小例子**
- $\bar R_p = 15\%, r_f = 3\%, \sigma_p = 10\%$ → Sharpe = $(15-3)/10 = 1.2$。
- 若波动从 10% 升到 20%（其他不变）→ Sharpe = $12/20 = 0.6$，直接腰斩。
- 若收益从 15% 升到 27%（其他不变）→ Sharpe = $24/10 = 2.4$，翻倍。

**实战直觉**：夏普是“每冒一份总风险，赚多少超额收益”。它把所有波动（上涨和下跌）都当成风险，所以对大涨大跌都很敏感。

---

### 8.2 索提诺比率 Sortino Ratio

$$\text{Sortino} = \frac{\bar R_p - r_f}{\sigma_{\text{downside}}}$$

**$\sigma_{\text{downside}}$ 到底是什么**
它是“下行标准差”，只计算**低于某个目标收益**（通常取 $r_f$ 或 0）的那部分收益的波动。上涨带来的波动被忽略，因为那是“好波动”。

计算步骤（以目标为 $r_f$ 为例）：
1. 对每个时期的收益 $R_i$，计算 $R_i - r_f$。
2. 只保留**负数**（即低于目标的部分），正数和零不算。
3. 对这些负数求标准差。

$$\sigma_{\text{downside}} = \sqrt{\frac{1}{T} \sum_{i=1}^{T} \min(R_i - r_f,\ 0)^2}$$

**关键区别**
- 夏普的分母 $\sigma_p$ 是**全部波动**：上涨 20% 和下跌 20% 都会被惩罚。
- 索提诺的分母 $\sigma_{\text{downside}}$ 只惩罚**下跌**：上涨 20% 不增加分母。

**变量增大/减小 → 指标怎么变 → 为什么**
- $\bar R_p$ **增大** → Sortino **增大**。
  - 为什么：分子变大，分母不变。
- $r_f$ **增大** → Sortino **减小**。
  - 为什么：分子 $(\bar R_p - r_f)$ 变小，同时低于 $r_f$ 的收益变多，$\sigma_{\text{downside}}$ 也可能变大，双重打压。
- $\sigma_{\text{downside}}$ **增大** → Sortino **减小**。
  - 为什么：分母变大，坏波动越多，风险调整后收益越差。
- 上涨波动（$\sigma_{\text{upside}}$）**增大** → Sortino **不变**。
  - 为什么：$\sigma_{\text{downside}}$ 根本不计算上涨的部分。
- 收益分布中“下跌尾”越厚 → $\sigma_{\text{downside}}$ 越大 → Sortino 越小。

**小例子**
组合 A：月收益 $[+20\%, -5\%, +15\%, -3\%, +10\%]$，$r_f = 0$。
- 平均收益 $\bar R_p = 7.4\%$。
- 低于 0 的收益：$-5\%, -3\%$。
- $\min(R_i - 0, 0)^2 = [0, 25, 0, 9, 0]$，均值 $= 34/5 = 6.8$。
- $\sigma_{\text{downside}} = \sqrt{6.8} \approx 2.61\%$。
- Sortino $= 7.4 / 2.61 \approx 2.83$。

组合 B：月收益 $[+5\%, -2\%, +4\%, -1\%, +3\%]$，$r_f = 0$。
- 平均收益 $\bar R_p = 1.8\%$。
- 低于 0 的收益：$-2\%, -1\%$。
- $\sigma_{\text{downside}} = \sqrt{(4+1)/5} = 1\%$。
- Sortino $= 1.8 / 1 = 1.8$。

组合 A 虽然总波动更大，但主要是“好波动”，所以 Sortino 反而更高。

**实战直觉**：如果你相信“上涨不是风险”，索提诺比夏普更公平。但 downside 的计算依赖于目标收益的选择（$r_f$ 还是 0），不同选择会得到不同结果。

---

### 8.3 回撤与最大回撤 Drawdown / Max Drawdown

$$\text{Drawdown}_t = \frac{\text{Peak}_t - \text{Current}_t}{\text{Peak}_t}, \quad \text{MDD} = \max_t \text{Drawdown}_t$$

**变量分别是什么**
- $\text{Peak}_t$：到时间 $t$ 为止的历史最高净值。
- $\text{Current}_t$：当前净值。
- $\text{Drawdown}_t$：当前净值从最高点跌了多少百分比。
- MDD：整个回测期内最大的那一笔跌幅。

**变量增大/减小 → 指标怎么变 → 为什么**
- $\text{Peak}_t$ **增大**（当前净值不变）→ Drawdown **减小**。
  - 为什么：分母变大，分子 $(\text{Peak} - \text{Current})$ 相对占比变小。
- $\text{Current}_t$ **增大**（接近 Peak）→ Drawdown **减小**。
  - 为什么：分子 $(\text{Peak} - \text{Current})$ 变小。
- $\text{Current}_t$ **下跌**（远离 Peak）→ Drawdown **增大**。
  - 为什么：分子变大，跌得越深回撤越大。
- 净值创新高 → $\text{Peak} = \text{Current}$ → Drawdown = 0。
- 净值长期横盘不创新高 → Drawdown 持续存在，策略看起来“还没爬出来”。
- MDD **本身越大** → 策略风险越大。
- 当 MDD 作为 Calmar 的分母时：MDD **增大** → Calmar **减小**。

**小例子**
净值序列：$1.0 \rightarrow 1.2 \rightarrow 1.1 \rightarrow 1.3 \rightarrow 1.0 \rightarrow 1.4$。
- 在净值 1.1 时：Peak = 1.2，Current = 1.1，Drawdown = $(1.2-1.1)/1.2 = 8.3\%$。
- 在净值 1.0 时：Peak = 1.3，Current = 1.0，Drawdown = $(1.3-1.0)/1.3 = 23.1\%$ ← 这是 MDD。
- 在净值 1.4 时：Peak = 1.4，Current = 1.4，Drawdown = 0。

**实战直觉**：回撤是“投资者真实感受到的痛”。一个策略夏普很高，但 MDD 很深，客户可能在最低点赎回，实际赚不到钱。衡量“能不能拿得住”比衡量“平均赚多少”更重要。

---

### 8.4 卡玛比率 Calmar Ratio

$$\text{Calmar} = \frac{\text{年化收益}}{\text{MDD}}$$

**变量分别是什么**
- 年化收益：策略一年赚多少（通常用几何平均年化，不是简单相加）。
- MDD：最大回撤的绝对值（比如 20%）。

**变量增大/减小 → 指标怎么变 → 为什么**
- 年化收益 **增大** → Calmar **增大**。
  - 为什么：分子变大，同样的最大痛苦换来更多收益。
- MDD **增大** → Calmar **减小**。
  - 为什么：分母变大，最大回撤越深，风险收益比越差。
- MDD **减小** → Calmar **增大**。
  - 为什么：回撤控制得好，策略“睡得着觉”。
- MDD 接近 0 → Calmar 会变得非常大。
  - 但要注意：可能是回测期太短没遇到大跌，不代表策略真的低风险。

**小例子**
- 年化收益 20%，MDD 20% → Calmar = 1.0。
- 年化收益 20%，MDD 10% → Calmar = 2.0（回撤控制更好，性价比翻倍）。
- 年化收益 10%，MDD 5% → Calmar = 2.0（收益低但风险更低，同样划算）。
- 年化收益 50%，MDD 5% → Calmar = 10.0（看起来极好，但大概率是短回测或过拟合）。

**实战直觉**：Calmar 把“赚多少”和“最痛时亏多少”直接挂钩。它比夏普更贴近真实持有体验。但用的时候要检查 MDD 是不是来自足够长的样本期。

---

### 8.5 信息比率 Information Ratio（组合层面）

$$\text{IR} = \frac{\bar R_p - \bar R_b}{\text{TE}}, \quad \text{TE} = \sigma(R_p - R_b)$$

**变量分别是什么**
- $\bar R_p$：组合平均收益。
- $\bar R_b$：基准平均收益（比如沪深 300、恒生指数）。
- $\bar R_p - \bar R_b$：主动收益 / 超额收益。
- TE（Tracking Error）：跟踪误差，即组合收益减去基准收益的**标准差**。
- IR：每冒一份“偏离基准”的风险，能带来多少超额收益。

**变量增大/减小 → 指标怎么变 → 为什么**
- $\bar R_p$ **增大**（$\bar R_b$ 不变）→ IR **增大**。
  - 为什么：主动收益 $(\bar R_p - \bar R_b)$ 变大，分子变大。
- $\bar R_b$ **增大**（$\bar R_p$ 不变）→ IR **减小**。
  - 为什么：主动收益变小，甚至可能变负。
- $\bar R_p$ 和 $\bar R_b$ **同向涨**，但 $\bar R_p$ 涨得更快 → IR **增大**。
  - 为什么：差额 $(\bar R_p - \bar R_b)$ 变大。
- $\bar R_p$ 和 $\bar R_b$ **同向跌**，但 $\bar R_p$ 跌得更快 → IR **减小**（甚至变负）。
- TE **增大** → IR **减小**。
  - 为什么：偏离基准的波动变大，主动风险变高。
- TE **减小**（组合紧跟基准）→ IR **增大**。
  - 但此时主动收益 $(\bar R_p - \bar R_b)$ 也可能变小，所以不一定代表策略更好。
- $\bar R_p = \bar R_b$ 时 → 主动收益 = 0，IR = 0（完全没跑赢）。
- TE = 0 且 $\bar R_p \neq \bar R_b$ 时 → IR 理论上无穷大，实际意味着组合是基准的恒定偏移，无主动管理意义。

**小例子**
- $\bar R_p = 15\%, \bar R_b = 10\%, \text{TE} = 5\%$ → IR = $(15-10)/5 = 1.0$。
- 若基准涨到 15%（组合不变）→ IR = 0。
- 若 TE 增加到 10%（组合相对基准波动更大）→ IR = 0.5。
- 若组合收益涨到 20%（基准 10%，TE 仍为 5%）→ IR = 2.0。

**关键区分**
- **IR（Information Ratio）**：组合层面，衡量组合相对基准的“稳定超额能力”。
- **ICIR（Information Coefficient IC Ratio）**：因子层面，衡量因子预测的稳定性。
- 两者缩写都叫“IR”，但维度完全不同。读论文和报告时一定要看上下文。

**实战直觉**：IR 是主动管理型产品的核心 KPI。它告诉你：你为了跑赢基准，冒的偏离风险值不值得。IR 高 = 稳定跑赢；IR 低 = 要么跑不赢，要么波动太大。

---

### 8.6 换手率 / 容量 / 交易成本（实盘会吃掉 alpha）

**核心公式**
$$\text{净收益} = \text{毛收益} - \text{交易成本}$$
$$\text{交易成本} = \text{佣金} + \text{印花税} + \text{滑点} + \text{市场冲击}$$

#### 8.6.1 换手率 Turnover

$$\text{Turnover} = \frac{\text{买入金额} + \text{卖出金额}}{2 \times \text{组合总资产}}$$
（有些定义会把买入+卖出直接除以总资产，而不除以 2；读报告时注意定义。）

**变量增大/减小 → 指标怎么变 → 为什么**
- Turnover **增大** → 交易成本 **增大**。
  - 为什么：交易金额多了，佣金和印花税按比例增加。
- Turnover **增大** → 市场冲击 **增大**。
  - 为什么：频繁买卖对市场价格造成更大扰动。
- Turnover **增大** → 净收益 **减小**。
  - 为什么：成本直接 subtract 收益。
- Turnover **太低** → 组合跟不上信号变化 → 可能错过 alpha，或风险敞口漂移。
- 所以 Turnover 不是越低越好，而是要在“跟踪信号”和“控制成本”之间平衡。

**小例子**
- 组合总资产 1 亿，每月买入 3000 万、卖出 3000 万 → Turnover = $(3000+3000)/(2\times10000) = 30\%$。
- 若买入 8000 万、卖出 8000 万 → Turnover = 80%，成本会显著上升。

#### 8.6.2 交易成本 Transaction Cost

交易成本由四部分组成：
1. **佣金**：券商收取，通常按成交金额比例（如万分之一到万分之五）。
2. **印花税**：政府收取，单边或双边（A 股卖出收 0.05%，港股双边）。
3. **滑点 Slippage**：信号发出时的价格 vs 实际成交价的差额。
   - 例如信号发出时股价 100 元，实际成交 100.2 元，滑点 = 0.2%。
4. **市场冲击 Market Impact**：大单买卖导致价格向不利方向移动。
   - 买入大单 → 把股价推高 → 买得更贵。
   - 卖出大单 → 把股价压低 → 卖得更便宜。

**变量增大/减小 → 指标怎么变 → 为什么**
- 佣金 **增大** → 净收益 **减小**。
  - 为什么：每多一笔交易都被抽成。
- 印花税 **增大** → 净收益 **减小**。
  - 为什么：政府直接切走一块利润。
- 滑点 **增大** → 实际成交价变差 → 净收益 **减小**。
- 市场冲击 **增大** → 尤其影响大容量策略 → 净收益 **减小**。
- 交易频率 **越高** → 滑点和冲击累计越严重。
- 股票流动性 **越差** → 滑点和冲击 **越大**。
- 下单执行算法 **越差** → 滑点和冲击 **越大**。

**小例子**
- 策略 A：毛收益 20%，换手率 50%，综合成本 1% → 净收益 19%。
- 策略 B：毛收益 30%，换手率 500%，综合成本 8% → 净收益 22%。
- 策略 B 毛收益更高，但成本吃掉了 8%。如果规模扩大 10 倍，冲击成本可能从 8% 升到 15%，净收益反而低于 A。

#### 8.6.3 容量 Capacity

定义：策略在不显著降低收益的前提下，能容纳的最大资金规模。

**变量增大/减小 → 指标怎么变 → 为什么**
- 资金规模 **增大** → 市场冲击 **增大**。
  - 为什么：同样的策略，用 10 亿买一只股票比 1000 万买会显著推升价格。
- 资金规模 **增大** → 滑点 **增大**。
  - 为什么：订单量变大，更难在理想价格全部成交。
- 资金规模 **增大** → 净收益 **减小**（超过甜蜜点后）。
  - 为什么：冲击成本把 alpha 吃掉。
- 资金规模 **太小** → 固定成本（系统、人力、数据）摊不平 → 不经济。
- 所以容量是一个“甜蜜点”：太大冲击成本吃不消，太小运营不划算。

**实战直觉**：很多论文里的高频小盘因子看起来 alpha 很高，真上规模就会失效。容量是“纸上富贵”到“真金白银”的分水岭。评估策略时一定要问自己：这个 alpha 在 10 亿、100 亿规模下还能剩多少？

---

### 8.7 信息系数 IC / Rank IC / ICIR（因子专用）

#### 8.7.1 IC（Pearson 信息系数）

$$\text{IC} = \text{corr}(\text{因子预测值}, \text{未来收益})$$

**变量分别是什么**
- 因子预测值：某股票在当前时点的因子得分（如动量、价值等）。
- 未来收益：下一个周期的收益率。
- corr：Pearson 线性相关系数。

**变量增大/减小 → 指标怎么变 → 为什么**
- 因子预测值与未来收益的线性相关程度 **增大** → IC **增大**（绝对值）。
  - 为什么：预测越准，相关系数越接近 1 或 -1。
- IC **为正且越大** → 因子方向正确：高分股票未来收益越高。
- IC **为负且绝对值越大** → 因子方向反了：高分股票未来收益越低，应该反向用。
- IC 接近 0 → 因子无效，预测和结果没关系。
- IC 的符号很关键：我们希望 IC > 0（正相关）。
- 样本期变短 → IC 波动变大，可靠性下降。
- 异常值存在 → IC 可能被扭曲，此时用 Rank IC 更稳健。

#### 8.7.2 Rank IC（Spearman 信息系数）

$$\text{Rank IC} = \text{corr}(\text{因子预测值排名}, \text{未来收益排名})$$

**变量敏感性**
- 和 IC 类似，但对极端值不敏感。
- 收益分布有肥尾或异常大波动时 → Rank IC 比 IC 更可靠。
- 因子预测值本身是排名或排序时 → Rank IC 更自然。

#### 8.7.3 ICIR（信息系数比率）

$$\text{ICIR} = \frac{\text{mean}(\text{IC})}{\text{std}(\text{IC})}$$

**变量增大/减小 → 指标怎么变 → 为什么**
- IC 均值 **增大** → ICIR **增大**。
  - 为什么：预测能力越强。
- IC 标准差 **增大** → ICIR **减小**。
  - 为什么：预测不稳定，时好时坏，实战不可靠。
- 因子衰减时：IC 均值下降，std 可能上升 → ICIR **下降**。
- ICIR 越大 → 因子越稳定有效。

**小例子**
- 因子 A：12 个月 IC 均值 = 0.05，std = 0.02 → ICIR = 0.05/0.02 = 2.5。
- 因子 B：12 个月 IC 均值 = 0.08，std = 0.06 → ICIR = 0.08/0.06 ≈ 1.33。
- 虽然 B 的平均预测能力更强，但 A 更稳定，实战可能更可靠。

**实战直觉**：IC 告诉你“这个因子预测方向对不对”，ICIR 告诉你“这个预测是不是一直对”。做因子组合时，宁可要平均 IC 低但稳定的因子，也不要平均 IC 高但忽正忽负的因子。

---

### 8.8 年化与复利陷阱

#### 8.8.1 几何年化收益

$$\text{年化收益} = (1 + \text{总收益})^{1/\text{年数}} - 1$$

**变量增大/减小 → 指标怎么变 → 为什么**
- 总收益 **增大** → 年化收益 **增大**。
  - 为什么：滚雪球的终值变大了。
- 年数 **增大**（时间拉长）→ 年化收益更“稳”，但短期爆发力被摊平。
  - 为什么：复利把短期运气平均掉了。
- 复利次数 **增多** → 终值增长加速（非线性）。
  - 为什么：利息再生利息，越滚越快。
- 用简单算术平均（总收益 / 年数）会**高估**年化收益，因为它忽略了复利。

#### 8.8.2 夏普年化

$$\text{年化 Sharpe} = \text{日 Sharpe} \times \sqrt{252}$$

**变量增大/减小 → 指标怎么变 → 为什么**
- 日 Sharpe **增大** → 年化 Sharpe **增大**（线性放大）。
- 观测频率从日变成周（n 从 252 变 52）→ 年化乘数从 $\sqrt{252}$ 变 $\sqrt{52}$，结果变小。
- 观测频率 **越高** → 年化乘数 **越大**。
  - 为什么：独立同分布假设下，方差可加，标准差按 $\sqrt{n}$ 缩放。
- 日收益之间如果高度自相关（不是独立的）→ $\sqrt{n}$ 规则会**高估**年化夏普。

#### 8.8.3 常见陷阱

- **错误 1**：用“收益率 × 252”来年化收益。这是算术平均，忽略了复利。
- **正确做法**：用几何平均 $(1+r)^{252} - 1$。
- **错误 2**：把日波动 × 252（而不是 × $\sqrt{252}$）。波动不是线性累加的。
- **正确做法**：年化波动 = 日波动 × $\sqrt{252}$。
- **错误 3**：混用不同频率比较策略。月 Sharpe 和日 Sharpe 不能比，除非都年化到同一频率。
- **错误 4**：252 是交易日假设。如果数据是周度，用 52；月度用 12。用错频率会系统性偏差。

**小例子**
- 日收益 0.05%，日波动 1%。
- 年化收益 $\approx (1.0005)^{252} - 1 \approx 12.9\%$（不是 $0.05\% \times 252 = 12.6\%$，两者接近但不同）。
- 日 Sharpe $= 0.05/1 = 0.05$ → 年化 Sharpe $= 0.05 \times \sqrt{252} \approx 0.794$。
- 年化波动 $= 1\% \times \sqrt{252} \approx 15.87\%$。

**实战直觉**：年化是“让不同频率的策略可比”的换算器。但复利和 $\sqrt{n}$ 是非线性的，很容易踩坑。所有策略汇报前，先把收益和波动都年化到同一频率再比较。

---
## 9. 特征值 / 特征向量 与 PCA 主成分分析

多因子模型的协方差 $\Sigma$ 是一个 $N\times N$ 的对称矩阵（§3）。当 $N$ 很大时，直接处理它既费算力又充满噪声。特征值 / 特征向量提供了"把矩阵拆成最本质的方向与强度"的工具，而 PCA 就是把它用在数据降维与风险分解上。本节从定义讲到它在 QR 里的三个实战用途，并标注每个关键量的"增大/减小会怎样"。

### 9.1 特征值与特征向量：定义

对一方阵 $A$，若存在非零向量 $v$ 和标量 $\lambda$ 使得

$$A v = \lambda v$$

则 $\lambda$ 称为 $A$ 的**特征值**（eigenvalue），$v$ 称为对应的**特征向量**（eigenvector）。

**几何直觉**：矩阵 $A$ 通常表示某种线性变换（旋转 + 拉伸）。特征向量是那些"被 $A$ 作用后**方向不变、只被拉长 / 压短**"的特殊向量；拉长 / 压短的倍数就是特征值 $\lambda$。
- $\lambda > 1$：该方向被拉伸。
- $0 < \lambda < 1$：该方向被压缩。
- $\lambda < 0$：方向翻转。

**为什么 QR 关心**：协方差矩阵 $\Sigma$ 的特征向量 = 风险的"主方向"——每个特征向量是一个"联合波动方向"（哪些股票一起涨一起跌），特征值就是这个方向波动的强度。这正是 §3 里 $w^\top\Sigma w$ 能拆成"各主方向独立波动之和"的数学依据。

### 9.2 对称矩阵的谱分解

对于**对称矩阵**（协方差矩阵 $\Sigma$ 天然对称），有非常干净的性质：
- 所有特征值 $\lambda_k$ 都是**实数**。
- 不同特征值对应的特征向量彼此**正交**。
- 把所有特征向量排成列组成正交矩阵 $Q$（满足 $Q^\top Q = I$），特征值放在对角阵 $\Lambda = \text{diag}(\lambda_1,\dots,\lambda_N)$ 上，则

$$\Sigma = Q \Lambda Q^\top$$

这就是**谱分解 / 对角化**：把任意对称矩阵拆成"正交基 × 对角强度 × 正交基转置"。$\Sigma$ 一旦被这样拆开，就退化成沿 $Q$ 各列方向、彼此独立、强度为 $\lambda_k$ 的波动。

**一个关键性质：特征值之和 = 迹 = 总方差**
$$\sum_{k=1}^{N}\lambda_k = \text{tr}(\Sigma) = \text{总方差}$$
即所有特征值加起来，恰好等于 $\Sigma$ 的对角线之和（各变量自身方差之和）。这就是为什么 §9.3 里"方差解释率 = $\lambda_k / \sum\lambda_i$"有意义——分母就是整个系统的总波动，分子是某一主方向切走的波动。

**变量增大/减小 → 指标怎么变 → 为什么**
- 某个特征值 $\lambda_k$ **增大** → 对应主方向的波动**变大**、该主成分对整体风险的贡献**变大**。
  - 为什么：$\lambda_k$ 直接就是该方向的方差，进 $\Sigma = Q\Lambda Q^\top$ 后放大那一维的波动。
- 特征值之间**越悬殊**（少数几个特别大）→ 数据越有"结构性"、越靠前的主成分能解释大部分方差（适合降维）；越接近**相等** → 数据越"弥散"、无主导方向（PCA 降维效果差）。
  - 为什么：方差解释率 $\lambda_k/\sum\lambda_i$ 的差距由 $\lambda_k$ 的相对大小决定。

**⚠️ 谱分解里什么确定、什么不确定（Q vs Λ）**

- **Λ 由 Σ 唯一确定（至多排列顺序）**：给定确切的对称矩阵 $\Sigma$，它的特征值集合 $\{\lambda_k\}$ 是唯一确定的——特征多项式 $\det(\Sigma-\lambda I)=0$ 的系数完全由 $\Sigma$ 的元素决定，其根（特征值）自然固定。你上面引的 $\text{tr}(\Sigma)=\sum\lambda_k$ 只是这 $N$ 个不变量之一（还有 $\det(\Sigma)=\prod\lambda_k$ 等），它们合起来把每个 $\lambda_k$ 都钉死。所以 $\Lambda=\text{diag}(\lambda_1,\dots,\lambda_N)$ 是确定的，差的只是对角元排列顺序（对应 $Q$ 的列置换）。
  - **推论**：PCA 的"各主成分解释方差比例 $\lambda_k/\sum\lambda$"在不同软件里完全一致、可复现——因为 $\lambda_k$ 由 $\Sigma$ 唯一决定。
- **Q 不唯一定（三种自由度）**：
  1. **符号翻转**：若 $Av=\lambda v$，则 $A(-v)=\lambda(-v)$，所以每一列可独立乘 $\pm1$；
  2. **列置换**：相等特征值对应的列可交换；
  3. **退化子空间内任意旋转**：若某 $\lambda$ 代数重数 $m>1$，其 eigenspace 维度为 $m$，里面**任何**一组标准正交基都合法 → 该子空间内有无穷多个 $Q$。
  - 一般协方差矩阵特征值互异，$Q$ 基本唯一（只差符号/排列）；这正是 PCA 主成分在不同软件里正负号可能不同、却代表同一轴的原因。
- **关键区分：$Q \neq B$**：谱分解的 $Q$ 永远正交（列彼此垂直）；但你多因子模型里的因子暴露矩阵 $B$（§6 的 $\Sigma=B V B^\top+\Delta$）**不要求正交**——不同因子可高度相关，$B$ 的列不必互相垂直。所以 $Q$ 是协方差的"自然正交基"，$B$ 是"你设计的因子载荷"（§9.4 对比 fundamental vs statistical 模型即此区别）。
- **现实修正（衔接 §9.5 / §10）**：上面成立的前提是"你手里是真实的 $\Sigma$"。QR 里你只有样本估计 $\hat\Sigma$（短样本、噪声大），所以经验 $\hat\Lambda$ 本身是带误差的估计，其小特征值几乎全是噪声（Marchenko-Pastur，§9.5）——这正是 Ledoit-Wolf（§10）要收缩、把噪声特征值拉回来的原因。

### 9.3 PCA 主成分分析：步骤

PCA（Principal Component Analysis）就是对数据的协方差矩阵做谱分解，并用它做降维。完整流程：

1. **中心化**：每个变量减掉自己的均值，使每列均值为 0。（若不中心化，第一主成分会偏向"均值最大"的方向，无意义。）
2. **算协方差矩阵 $\Sigma$，还是相关系数矩阵？** 两者只差一步：相关矩阵 = 先把每列标准化（§5）再算协方差。选哪个会**直接改变主成分本身**：
   - 用**协方差矩阵**：大方差变量（如市值、价格）天然主导第一主成分，小方差因子被压住。
   - 用**相关矩阵**：各变量被强制到单位方差，相当于给每个变量平等投票权，第一主成分更"均衡"。
   - 实战：因子量纲差异大（市值 vs 市盈率）时常用相关矩阵或先 z-score；只想保留原始波动结构时用协方差矩阵。
3. **特征分解** $\Sigma = Q\Lambda Q^\top$，按 $\lambda_1 \ge \lambda_2 \ge \dots$ 从大到小排序。
4. **取前 $K$ 个主成分**：保留前 $K$ 个特征向量（主成分方向），其余丢弃。
5. **投影**：把原始数据 $X$ 投影到这 $K$ 个方向上，得到降维后的坐标 $Z = X Q_{[:,1:K]}$。

**计算实现：PCA 通常用 SVD，不直接分解协方差**
数值上，PCA 几乎总是对**数据中心化后的矩阵 $X$**（每行一个样本、每列一个变量）做**奇异值分解（SVD）**：
$$X = U S V^\top$$
其中右奇异向量 $V$ 的列，正是协方差矩阵 $X^\top X$（差一个 $1/(n-1)$ 缩放）的**特征向量**；奇异值 $S$ 与特征值满足 $\lambda_k = s_k^2/(n-1)$。所以：
- **协方差特征分解** 和 **数据矩阵 SVD** 给出的是同一组主成分方向，只是计算的路子不同。
- 用 SVD 的好处：不用先显式算 $N\times N$ 协方差（当样本数 $T \ll N$ 时协方差甚至不满秩），直接对 $T\times N$ 数据分解更稳、更快。
- 这也解释了 sklearn 里 `PCA` 默认 `svd_solver='auto'`、numpy 里 `np.linalg.svd` 的用法。

**第 $k$ 个主成分解释的方差比例**：

$$\text{ExplainedVar}_k = \frac{\lambda_k}{\sum_{i=1}^{N}\lambda_i}$$

前 $K$ 个主成分累计解释比例就是 $\sum_{k=1}^{K}\lambda_k / \sum\lambda_i$。

**变量增大/减小 → 指标怎么变 → 为什么**
- 保留的主成分数 $K$ **增加** → 累计解释方差比例**增大**（能还原更多原始信息）。
  - 为什么：每多取一个主成分，就多累加一份 $\lambda_k$。
  - 但 $K$ 越大 → 也把**噪声成分**一起保留了 → 过拟合 / 拟合噪声的风险**增大**。
    - 为什么：真实信号往往集中在前几个大特征值，后面小的常常是估计误差（样本噪声）。
- 数据**未标准化**（直接对原始量纲做 PCA）→ 量纲大、方差大的变量（如市值）会**主导第一主成分**，淹没其他因子。
  - 为什么：PCA 按方差大小排方向，不标准化时大数值变量的方差天然碾压小数值变量。
  - 实战：做 PCA 前先 z-score 标准化（§5），保证每个变量公平贡献。

**怎么选 $K$：碎石图（Scree Plot）**
把特征值从大到小画成柱状图，找"拐点"（elbow）——拐点之后特征值趋于平缓，说明后面的成分主要是噪声。累计解释方差达到目标（如 80%–95%）所需的 $K$ 也是常用停点。
- $K$ **太小**（拐点前就停）→ 丢了大量信号 → 降维后信息损失大。
- $K$ **太大**（拐过后还取）→ 把噪声当信号 → 过拟合（见上方"变量增大/减小"）。

### 9.4 PCA 在量化里的三个实战用途

**(a) Statistical 因子模型（替代 fundamental 因子）**
Barra / AlphaStream 用的是 fundamental 因子模型：因子由人定义（价值、动量…）。PCA 提供另一条路——**让数据自己吐出因子**：对股票收益协方差做 PCA，前几个主成分就是"数据中最重要的共同波动方向"，可直接当作统计因子。
- 优点：全自动、正交、无前视假设。
- 缺点：**没有经济标签**（第一主成分常是"市场因子"，但第二、第三是什么没人说得清），可解释性差、不易做风险归因。

**(b) 风险主成分分解（Risk PCA）**
对收益协方差 $\Sigma$ 做 PCA，第一主成分通常对应**全市场系统性波动**（所有股票同涨同跌的方向），第二、第三可能是行业 / 风格板块。可用于：
- 判断组合是否过度暴露在某个隐藏主成分上。
- 作为压力测试：模拟"第一主成分跌 3 个标准差"时组合的损失。

**(c) 因子降维与去噪**
当你有几十个高度相关的候选因子（§6），直接进 $B$ 会共线、让 $\Sigma$ 病态。用 PCA 把它们压成几个正交主成分，既去冗余又去噪。这也常用于**因子正交化**——把原始因子投影到正交基上，消除因子间相关性（具体做法见 §15 因子预处理）。

**与多因子模型的关系**：$\Sigma = B V B^\top + \Delta$（§6）是"人定因子"；PCA 的 $\Sigma = Q\Lambda Q^\top$ 是"数据定因子"。两者都是把 $N\times N$ 的 $\Sigma$ 用低秩结构近似，只是因子来源不同——一个来自经济逻辑，一个来自数据本身。

### 9.5 局限与注意

- **解释性弱**：主成分是原始变量的线性组合，系数看着像"黑箱"，难向投资委员会解释。
- **对异常值敏感**：PCA 基于二阶矩（方差 / 协方差），一个极端日会把第一主成分拉歪 → 做 PCA 前务必 winsorize（详见 §15 因子预处理）。
- **线性假设**：PCA 只能捕捉线性相关性，非线性结构抓不到（需核 PCA 等）。
- **样本质量**：协方差估计本身需要足够样本（衔接 §10 Ledoit-Wolf 收缩——当样本不足时，对 $\Sigma$ 做收缩比硬做 PCA 更稳）。

**怎么区分"信号特征值"和"噪声特征值"：随机矩阵理论（Marchenko-Pastur）**
当 $T$（样本数）和 $N$（变量数）都很大时，即使数据**完全随机**（纯噪声），协方差矩阵也会自然长出一些"看似不小"的特征值。Marchenko-Pastur 分布给出了纯噪声矩阵特征值的理论上下界（假设变量已标准化到单位方差 $\sigma^2=1$）：
$$\lambda_{\min}^{\text{noise}} \le \lambda \le \lambda_{\max}^{\text{noise}}, \quad \lambda_{\max}^{\text{noise}} = \left(1+\sqrt{N/T}\right)^2$$
凡是落在这个区间内的特征值，大概率只是估计噪声，**不应当作真因子**；只有明显超出上界 $\lambda_{\max}^{\text{noise}}$ 的，才可能是真实结构性因子。
- **变量增大/减小 → 指标怎么变 → 为什么**
  - $N/T$（变量相对样本的比例）**增大** → 噪声特征值上界 $\lambda_{\max}^{\text{noise}}$ **增大** → 越难分辨信号与噪声。
    - 为什么：样本越"薄"（相对变量数），协方差估计越靠噪声撑起，假信号越多。这正是少样本下必须做 Ledoit-Wolf 收缩（§10）的根本原因。

---

## 10. 收缩估计：Ledoit-Wolf 与协方差去噪

§3 的协方差矩阵 $\Sigma$ 和 §9 的 PCA 都依赖一个前提：我们能**可靠地估计**协方差。但实战里这个前提经常塌方——尤其当样本数 $T$ 相对变量数 $N$ 偏小时。Ledoit-Wolf 收缩就是解决"样本协方差又噪又病态"的标准工具，也是 §6 风险模型里 $V$（因子协方差）和 $\Sigma$ 能稳下来的关键。

### 10.1 为什么样本协方差会塌方

样本协方差（无偏估计）$S = \frac{1}{T-1}\sum_{t}(r_t-\bar r)(r_t-\bar r)^\top$ 在 $T$ 足够大时是好的。但量化里常见反例：
- 港股几百只股票（$N$ 大），可用历史日频数据只有 3–4 年（$T \approx 750$–$1000$）→ $T$ 相对 $N$ 并不宽裕。
- 若用分钟级或因子数 $K$ 很大，$T < N$ 都会出现 → $S$ 甚至**不满秩、不可逆**，最小方差优化 $\min w^\top\Sigma w$（§12）直接无解或爆掉。

更隐蔽的问题是**噪声放大**（衔接 §9.5）：样本协方差的特征值里混着大量"估计噪声"（Marchenko-Pastur 区间内的假信号）。拿这种 $\Sigma$ 去优化，优化器会把权重押在噪声方向上，样本内漂亮、样本外崩盘。

**变量增大/减小 → 指标怎么变 → 为什么**
- 样本数 $T$ **减小**（相对 $N$）→ 样本协方差的**噪声增大**、条件数（最大/最小特征值比）**恶化**、越可能不可逆。
  - 为什么：每个协方差估计量是 $O(1/\sqrt{T})$ 噪声，T 小则噪声主导。
- 变量数 $N$ **增大** → 同样 $T$ 下噪声更重、更可能 $T<N$ 不可逆。
  - 为什么：要估的参数是 $O(N^2)$，样本却只有 $T$ 个观测，N 越大越不够。

### 10.2 收缩的核心思想：把"纯样本"往"结构化目标"拉

Ledoit-Wolf 的想法一句话：**别完全信样本协方差 $S$，也别完全信一个简单假设 $F$，而是取两者的加权平均**：

$$\hat\Sigma = \delta F + (1-\delta)\, S$$

- $S$：样本协方差（数据驱动，但噪）。
- $F$：**结构化目标（target / shrinkage target）**，一个"我们假定大概长这样"的干净矩阵（有偏但方差小）。
- $\delta \in [0,1]$：**收缩强度（shrinkage intensity）**，调"信数据多少 vs 信假设多少"。

**常见的目标 $F$ 怎么选**
- **恒相关模型（constant correlation，Ledoit-Wolf 2004 原版）**：$F$ 用样本中各变量的方差，但把所有变量对之间的相关系数强制设为**全样本的平均相关系数**。即"每只股票波动各异，但彼此相关性都一样"。
- **单指数模型（single-index）**：用市场因子解释一切相关性，$F_{ij} = \beta_i\beta_j\sigma_m^2$。
- **缩放单位阵（scaled identity）**：最粗暴，$F = \bar\sigma^2 I$（所有变量同方差、零相关），只保留平均波动水平。

直觉：$F$ 是"先验"，$S$ 是"数据"，$\hat\Sigma$ 是贝叶斯式折中——这正是 §6 风险模型"用结构化先验稳住估计"的体现。

### 10.3 收缩强度 $\delta$：怎么定、随什么变

$\delta$ **不是拍脑袋**，Ledoit-Wolf 推导出一个**解析最优 $\delta^*$**，目标是让 $\hat\Sigma$ 与真实协方差的均方误差（MSE）最小。它综合了：
- 样本协方差的**方差**（噪不噪）—— 越噪越该多收缩；
- 目标 $F$ 与样本 $S$ 的**偏离程度** —— 偏离越大，F 越不可信，越少收缩；
- 样本量 $T$ —— 越大越信样本。

**变量增大/减小 → 指标怎么变 → 为什么**
- 样本数 $T$ **增大** → 最优 $\delta^*$ **减小**（更信样本 $S$）。
  - 为什么：T 大时样本协方差本身够稳，不需要往目标拉。
- 样本协方差的**噪声增大**（如 $N/T$ 上升，见 §9.5）→ 最优 $\delta^*$ **增大**（更信目标 $F$）。
  - 为什么：数据不可靠时，结构化先验更稳。这正是 §9.5 里 $N/T$ 越大越难分信号噪声的同一根因。
- $\delta = 0$ → 退化为纯样本 $S$（无收缩，噪）。
- $\delta = 1$ → 退化为纯目标 $F$（完全结构化，可能欠拟合、有偏）。

**极端提醒**：$\delta$ 太大 → 模型被目标 $F$ 绑架，丢失真实结构（欠拟合）；$\delta$ 太小 → 等于没收缩，噪声照旧。最优 $\delta^*$ 自动在这两端取平衡。

### 10.4 特征值收缩（Ledoit-Wolf 2012 / 随机矩阵变体）

还有一种更"对症下药"的写法，直接收拾 §9.5 的噪声特征值：不把整块矩阵往 $F$ 拉，而是**把每个特征值单独往均值收缩**（示意性写法）：

$$\hat\lambda_k = \delta \bar\lambda + (1-\delta)\lambda_k$$

或更现代的非线性收缩（Ledoit-Wolf 2012 "nonlinear shrinkage"），对每个样本特征值 $\lambda_k$ 套一个解析映射 $\hat\lambda_k = \eta(\lambda_k)$，把位于 Marchenko-Pastur 噪声带里的特征值压向带内、把超出上界的真信号特征值基本保留。这样：
- 噪声特征值被压低 → 优化器不再押注噪声方向；
- 真因子特征值几乎不动 → 信号保留。
这把 §9.5 的"判据"变成了"自动修正"。

### 10.5 在 QR / 多因子模型里怎么用

回到 §6 的分解 $\Sigma = B V B^\top + \Delta$：
- **收缩因子协方差 $V$**（最常见、最推荐）：因子数 $K$ 通常不大（几十），但样本同样有限。对 $V$ 做 Ledoit-Wolf，得到稳的 $F$。这正是 COLLABORATION_PLAN 里"Ledoit-Wolf 收缩 V"的出处。
- **收缩特异方差 $\Delta$**：个股特异收益协方差通常假设对角，但对角元素本身也该用收缩（如把个股方差往行业/市值组均值拉）防止单只股票异常。
- **直接收缩 $\Sigma$**：当 $N$ 不大时也能直接对 $N\times N$ 做，但 $N$ 大时更优做法是先收缩 $V$ 再合成。
- **衔接你项目的坑**：COLLABORATION_PLAN 评审里标红的"Kani 直接 `LedoitWolf(X)` 在暴露上"——正确做法是先有收益 $r$ → 横截面回归出 $f,\varepsilon$（§6.3）→ 再对 $V=\text{cov}(f)$ 做 Ledoit-Wolf 收缩。跳过 $r$ 直接收缩暴露，等价于用错了输入。

### 10.6 实战验证与注意

- **怎么知道收缩有用**：看样本外表现，而非样本内。常用指标：
  - 样本外**似然度（out-of-sample likelihood）**更高 = 估计更准。
  - 组合**实现波动率 vs 预测波动率**的偏差更小。
- **工具**：sklearn `sklearn.covariance.LedoitWolf`（自动估 $\delta^*$）；`sklearn.covariance.OAS`（Oracle 近似收缩，Ledoit-Wolf 的改进版，对中等 $T$ 更稳）。
- **不是万能**：收缩解决"估计噪声"，但不解决"因子选错""前视泄漏""成本没算"——那些是 §11 / §12 / 回测（COLLABORATION_PLAN Phase 2.5）的事。

---

## 11. 假设检验：t 值 / IC 显著性 / 多重检验 / Deflated Sharpe

因子和策略的"显著性"不能靠肉眼看曲线——你必须用统计检验回答"这个效果是不是只是运气/噪声"。这一节把回测和因子验证里的假设检验拆开，每个量都给出**变量增减 → 结论怎么变 → 为什么**。

### 11.1 假设检验的基本框架

- **原假设 $H_0$**：默认成立的"无效"状态。在量化里通常是"因子没有预测能力 / 收益来自噪声"（$H_0: \beta=0$ 或 $H_0: \text{IC}=0$）。
- **备择假设 $H_1$**：你想证明的"有效"状态（$\beta\neq0$ / $\text{IC}\neq0$）。
- **p 值**：在原假设成立的前提下，观察到当前（或更极端）结果的概率。$p$ 越小，$H_0$ 越站不住脚。
- **显著性水平 $\alpha$**：你预先划的红线（常取 0.05）。若 $p<\alpha$ 就**拒绝 $H_0$**，认为效果"统计显著"。

**变量敏感性**
| 变量 | 增大 → | 为什么 |
|---|---|---|
| 证据强度（如 t 值） | 越易拒绝 $H_0$ | 信号相对噪声越强，越不像偶然 |
| $\alpha$（红线放宽） | 越易拒绝 $H_0$ | 容错门槛低了，但也更易把噪声当信号（假阳性↑） |
| 检验次数 $m$ | 越易出现假阳性 | 见 11.4 多重检验 |

**实战直觉**：显著性 ≠ 经济显著。一个 IC=0.005 的因子可能统计显著（样本超大），但年化贡献几乎为零——所以 §8 的 IC 和这里的显著性要一起看。

### 11.2 t 值与显著性

对回归系数 $\hat\beta$，检验"它是否为 0"用 t 统计量：
$$t = \frac{\hat\beta}{\text{SE}(\hat\beta)}$$
- $\hat\beta$：估计出的系数（如 CAPM 的 $\beta_i$、某因子的载荷）。
- $\text{SE}(\hat\beta)$：**标准误**，衡量这个估计有多不确定（样本噪声越大、样本量越小，SE 越大）。
- 自由度 $df = n - k$（$n$ 样本量、$k$ 参数个数）。大样本下，临界值约 **$|t| > 2$** 对应 $p<0.05$ 显著。

**变量敏感性**
| 变量 | 增大 → | 为什么 |
|---|---|---|
| 系数 $\hat\beta$ | $t$ 增大 | 信号变强，更易显著 |
| 标准误 SE | $t$ 减小 | 估计越不确定，信号被噪声稀释 |
| 样本量 $n$ | SE 减小 → $t$ 增大 | 大样本下 SE $\propto 1/\sqrt{n}$，信息更密，更易揪出真信号 |
| 参数个数 $k$ | 自由度↓ → 临界值↑ | 模型越复杂，越难达到显著（惩罚过拟合） |

**数值例子**：某因子载荷 $\hat\beta=0.03$，SE=0.008 → $t=3.75 > 2$，显著。若 SE 因样本短涨到 0.02 → $t=1.5$，不显著了。**这正是短样本港股数据的痛点**——`COLLABORATION_PLAN` 里 `validate_factors.py` 把 $|t|>2$ 作为因子入 X 的门槛，就来自这条规则。

### 11.3 IC / Rank IC 的显著性检验

因子 IC 在时间序列上的稳定性可用 t 检验（回顾 §6.3 / §8.7）：
$$t_{\text{IC}} = \frac{\overline{\text{IC}}}{\text{std}(\text{IC})} \cdot \sqrt{T} = \text{ICIR} \cdot \sqrt{T}$$
- $\overline{\text{IC}}$：IC 的时序均值（因子平均预测力）。
- $\text{std}(\text{IC})$：IC 的时序波动（稳定性反面）。
- $T$：回测期月数/天数。
- 当 $|t_{\text{IC}}| > 2$，认为因子 IC 显著不为 0。

**变量敏感性**
| 变量 | 增大 → | 为什么 |
|---|---|---|
| 平均 IC | $t_{\text{IC}}$ 增大 | 预测力越强 |
| IC 波动 std | $t_{\text{IC}}$ 减小 | 时灵时不灵，平均贡献被稀释 |
| 回测期 $T$ | $t_{\text{IC}}$ 增大（$\propto\sqrt{T}$） | 观察越久，偶然性越被平均掉 |
| Rank IC | 通常 $|t|$ 比 Pearson IC 更稳 | 排名法抗异常值，SE 更小 |

**实战直觉**：一个因子 IC 均值高但波动大（std 也大），$t$ 可能不显著——它"偶尔很灵"，但不可靠。ICIR（=均值/std）本身就是这条检验的核心，所以 §8.7 把它和 §11 连起来看。

### 11.4 多重检验：为什么测很多因子会"假阳性"爆炸

**问题**：你同时检验 $m$ 个因子，每个显著性 $\alpha=0.05$。整体（Family-wise）至少出一个假阳性的概率：
$$P(\text{至少一次假阳性}) = 1 - (1-\alpha)^m$$
测 20 个因子 → 概率已到 $1-0.95^{20} \approx 64\%$。**你"发现"的有效因子，很可能只是噪声被反复抽样撞出来的。**

**校正方法**
1. **Bonferroni**：把每个检验的阈值收紧到 $\alpha/m$。保守，假阳性少但易漏真因子（功效低）。
2. **FDR / Benjamini-Hochberg**：控制"被拒的假设里假阳性的比例"，比 Bonferroni 更有功效，是组学/因子筛选主流。
3. **Hold-out / 样本外再验**：把"看起来显著"的因子拿到独立样本再测一次，过不了就砍。

**⚠️ p-hacking / 数据窥探偏差（data snooping）**
- 反复换参数、换股票池直到某组合"显著"，本质是**把同一份数据当训练又当验证**，p 值已失真。
- 缓解：预注册假说（你 `QUANT_RESEARCH_GUIDE` 的"研究假说"第一步就是为此）、walk-forward（§14）、Deflated Sharpe（11.5）。
- **变量敏感性**：检验/调参次数 $m$ 增大 → 假阳性概率↑、真实 p 值被低估 → 结论可信度↓。

### 11.5 Deflated Sharpe Ratio（去偏夏普）：过拟合后的夏普还信不信

普通夏普高估回测表现（因为你在样本里挑了最优参数）。DSR 把"测试了多少策略/参数"纳入修正：
$$\text{DSR} \text{ 检验 } H_0: \text{真实夏普} \le \text{基准夏普}$$
- 核心输入：**回测次数 $N$（你试过的策略数）**、夏普估计误差（依赖样本期、偏度、峰度）。
- 直觉：测了 1000 个策略，其中一个夏普=2 一点不稀奇；DSR 会把这个"运气成分"扣掉，给出"扣除过拟合后还显著吗"。
- 与 §8.1 的 Deflated Sharpe 呼应：那是结论，这里是**检验本身**。

**变量敏感性**
| 变量 | 增大 → | 为什么 |
|---|---|---|
| 回测/调参次数 $N$ | 去偏后更不显著 | 试得越多，"高夏普"越可能是抽样运气 |
| 样本期 $T$ | 夏普估计更准、DSR 更稳 | SE 随 $\sqrt{T}$ 下降 |
| 真实夏普 | DSR 越显著 | 信号本身强 |

### 11.6 局限与 ⚠️ 注意

- **显著性依赖假设**：t 检验假定收益近似正态、独立同分布。肥尾、自相关（见 §14）会让 SE 失真 → t 偏大 → 假显著。
- **p 值不是"效应大小"**：显著的因子可能经济意义为零（11.1）。
- **不能替代样本外**：假设检验只在你"看"的数据上成立；真正检验泛化靠 walk-forward + OOS（§14），DSR 只是事后补偿。
- **和 §8/§10 的衔接**：Sharpe 显著性（§8）→ DSR（§11.5）；因子 IC 显著性直接喂给 `validate_factors`（§6.3、COLLABORATION_PLAN）；过拟合防护=多重检验 + DSR + walk-forward 三件套。

---

## 12. 最优化：Markowitz 均值方差与约束求解

组合优化就是"在约束下，找一组权重 $w$ 让组合最好"。这是 §6 风险模型、§8 绩效指标的最终落点——所有前面的矩阵都是为了喂给优化器。

### 12.1 均值方差框架（Markowitz）

组合有两个目标，天然冲突：
- **收益**：$E[R_p] = w^\top \mu$（$\mu$ 是各资产期望收益向量）
- **风险**：$\text{Var}(R_p) = w^\top \Sigma w$（$\Sigma$ 就是 §3/§6 的协方差矩阵）

Markowitz 的核心思想：**不在"高收益"或"低风险"里二选一，而是在给定风险下追最高收益（或给定收益下压最低风险）**。把所有"风险–收益"最优组合连起来，就是**有效前沿（efficient frontier）**。前沿左上端点 = 最小方差组合；与资本配置线相切的点 = 最大夏普组合（§8.1）。

### 12.2 目标函数

最通用的均值方差目标（求最大）：
$$\max_w\; w^\top \mu - \frac{\delta}{2}\, w^\top \Sigma w$$
- $\delta$：**风险厌恶系数**，越大越怕风险。
- 当 $\delta \to \infty$（或 $\mu=0$）→ 退化为**最小方差组合**：
  $$\min_w\; w^\top \Sigma w$$
  这正是 AlphaStream 用的目标（`min_variance(cov, ...)`）。最小方差对 $\mu$ 不敏感，比"最大夏普"更鲁棒（见 12.7）。

### 12.3 约束条件（实务里缺一不可）

| 约束 | 数学 | 为什么 |
|---|---|---|
| 预算约束 | $\sum_i w_i = 1$（或 $=0$ 表示市场中性） | 资金全投出 / 多空对冲 |
| 多空 | $w_i$ 可负 | 允许做空；纯多头要 $w_i \ge 0$ |
| 权重上下限 | $l_i \le w_i \le u_i$ | AlphaStream 的 `max_pos=0.3` 即 $u_i=0.3$ |
| 行业中性 | $B_{\text{ind}}^\top w = 0$ | 不让组合偷偷押注某行业（§6 中性化同源） |
| 换手约束 | $\|w - w_{\text{prev}}\|_1 \le \tau$ | 控制调仓量，压交易成本（§8.6） |
| 交易成本 | 在目标里减 $\kappa \cdot \|w-w_{\text{prev}}\|_1$ | 佣金+印花税+滑点+冲击，否则收益虚高 |

**⚠️ 衔接你的项目**：`COLLABORATION_PLAN.md` 的 `min_variance(cov, max_pos=0.3)` 只加了权重上限，**缺交易成本 / 换手约束**——这正是评审时标红的缺口（收益会虚高）。且这里的 `cov` 必须来自 §6 的因子模型 $\Sigma = X V X^\top + \Delta$，而非直接估全样本 $\Sigma$。

### 12.3.1 常见目标之二：风险平价（Risk Parity）

除均值方差 / 最小方差外，风险平价是实务里最常用的第三类目标——它追求**每个资产对组合总风险的贡献相等**，而非收益最高或方差最小。

- **单个资产的风险贡献（Risk Contribution, RC）**：
  $$RC_i = w_i \cdot \frac{(\Sigma w)_i}{\sqrt{w^\top \Sigma w}}$$
  分子 $w_i(\Sigma w)_i$ 是资产 $i$ 对组合方差的边际贡献，分母归一化到总风险。
- **风险平价要求**：对所有 $i,j$ 都有 $RC_i = RC_j$，即各资产风险贡献占比 $= 1/N$。
- **直觉**：市值加权等权会让少数大票主导风险；风险平价强制分散，对单一资产崩盘更鲁棒。
- **变量敏感性**：
  - 某资产波动率↑ → 其权重自动↓以维持等贡献；
  - 资产数 $N$ 增大 → 单资产权重上限↓（每个只摊 $1/N$ 风险）。
- **求解**：通常是**非凸**问题，常用迭代算法（如 cyclical coordinate descent）或凸近似。
- **衔接**：风险平价和你 §6 的因子风险归因**同源**——都是把组合风险拆回各成分（这里拆回各资产，那里拆回各因子）。

### 12.4 变量增大/减小 → 结论怎么变 → 为什么

| 变量 | 增大 → | 为什么 |
|---|---|---|
| 风险厌恶 $\delta$ | 权重更偏向低方差资产、组合风险↓ | 目标里风险项权重变大，优化器主动避险 |
| 某 $\Sigma_{ij}$（i,j 协方差） | 优化器降低 i、j 同方向敞口 | 持有两者会叠加风险，故减配其一或反向 |
| 某资产期望收益 $\mu_i$ | 权重 $w_i$ ↑ | 高收益资产更划算 |
| 交易成本 $\kappa$ | 换手↓、权重更接近上期 | 调仓代价变高，优化器选择"少动" |
| 权重上限 $u_i$ | 单股集中度↓、更分散 | 约束收紧，被迫分散 |

### 12.5 怎么求解

- **无约束解析解**（仅教学用）：对 $\max w^\top\mu - \frac{\delta}{2}w^\top\Sigma w$ 求导置零，得 $w^* \propto \Sigma^{-1}\mu$。
  - **⚠️ 隐含条件**：算出来后**必须归一化**（$\sum w$ 未必 = 1，除非预算约束已进目标）；且它要求 $\Sigma$ **可逆**——不可逆（或近奇异）时直接崩，须先做 §10 收缩或加下面正则化。
  - 加了约束（尤其上下限、换手）后**无闭式解**，必须走数值优化。
- **凸优化 / 二次规划（QP）**：目标 $w^\top\Sigma w$ 在 $\Sigma$ 半正定（§3）时是凸的，加线性约束后可保证找到全局最优。主流用 `cvxpy` / `scipy.optimize.minimize(method="SLSQP")`。
- **为什么 AlphaStream 用 SLSQP**：它支持**非线性目标 + 线性/非线性约束**，能直接把 `max_pos`、`行业中性`、`换手` 写进约束字典，无需把问题改写成标准 QP。
- **⚠️ 最大夏普不是凸问题**：$\max w^\top\mu/\sqrt{w^\top\Sigma w}$ 目标非凸，直接优化易陷局部解。两个标准解法：
  - **凸重构（推荐）**：对变量重参数化——令 $y = w/(w^\top\mu)$、$\kappa = 1/(w^\top\mu)$，原问题改写成等价 QP 后求解，比"先解前沿再挑"更干净可靠。
  - **前沿法**：先解最小方差前沿、再在前沿上挑夏普最高者（§8.1）。
- **权重正则化（抑制估计误差放大）**：在目标里加 L2 惩罚 $\lambda\|w\|^2$，即 $\min_w w^\top\Sigma w + \lambda\, w^\top w$。这把极端权重往均等方向拉，是对 §10 协方差收缩的"双保险"——一个修输入 $\Sigma$，一个修输出 $w$。
- **⚠️ Σ 的病态 / 条件数**：若 $\Sigma$ 有几个几乎相等的小特征值（§9.2 退化子空间），其逆 $\Sigma^{-1}$ 数值炸裂，min-variance 权重会失控。这正是 §10 Ledoit-Wolf 收缩要救的同一件事——收缩把噪声特征值拉离 0，优化器才稳。

### 12.6 局限与 ⚠️ 注意

- **垃圾进垃圾出（最关键的坑）**：优化器对输入极度敏感。$\Sigma$ 若有估计误差（§9.5/§10 的噪声特征值），权重会被放大到错误资产上；$\mu$ 更是难估、误差最大。所以**最小方差比最大夏普更鲁棒**（前者主要吃 $\Sigma$，后者还吃 $\mu$）。
- **估计误差放大**：无约束最优权重常出现极端值（满仓某只、大幅做空），必须靠 §12.3 的上下限/正则化压住。
- **前视泄漏**：$\mu$、$\Sigma$ 若用了未来数据，优化出的权重是"作弊"的——必须 walk-forward（§14）+ 样本外验证。
- **非凸约束**：加交易成本、整数持仓（只能买整百股）会让问题变非凸，需启发式/近似。

---

## 13. 常用机器学习方法（QR 场景）

经典多因子（§6）本质是"人定因子 + 线性回归"。当因子非线性、因子间有交互、或想直接从海量数据挖信号时，机器学习（ML）登场。ML 在 QR 里不是替代多因子，而是它的**超集 / 补充**：预测目标（收益、方向、波动率）、构造非线性因子、做聚类与降维。

### 13.1 为什么 QR 用 ML（以及它的特殊风险）
- **优势**：能捕捉**非线性**与**特征交互**（如"低估值 + 高动量"共振），不要求你先手定因子形式。
- **特殊风险**（比一般 ML 更致命）：
  - **前视泄漏**：金融标签（未来收益）极易在特征工程时被污染（§12.6 / §14）。一个错 feature 就能让回测炸裂。
  - **非平稳**：市场规律会变，训练好的模型易失效（因子衰减，见指南）。
  - **样本少、噪声大**：日频数据几百到几千样本、噪声占比高，过拟合极容易。
  - **幸存者偏差**：用"还在上市"的股票训练，模型会误以为退市不可能。

### 13.2 线性模型（baseline，永远先跑）
- **OLS 回归**（§4）：单因子 / 多因子 baseline。
- **岭回归 Ridge**（L2）：$\min_\beta \|y-X\beta\|^2 + \lambda\|\beta\|^2$ —— 系数 shrinkage，防共线、降方差（与 §12.5 权重正则化同源）。$\lambda$ 增大 → 系数更收缩 → 模型更保守。
- **Lasso**（L1）：$\min_\beta \|y-X\beta\|^2 + \lambda\|\beta\|_1$ —— 系数可**精确归零**，做自动特征选择（剔除无效因子）。$\lambda$ 增大 → 更多系数变 0。
- 直觉：线性模型可解释、不易过拟合，是 ML 流程的"对照组"。

### 13.3 树模型（QR 最常用 ML）
- **决策树 / Random Forest / Gradient Boosting（XGBoost、LightGBM、CatBoost）**。
- 为何 QR 爱用 GBM：自动处理**非线性 + 特征交互**，对缺失值 / 量纲不敏感，训练快。
- 变量敏感性：$\text{max\_depth}$ 增大 → 模型更复杂 → 拟合能力↑但过拟合风险↑；$\text{learning\_rate}$ 减小 → 需更多树、更平滑；$\text{n\_estimators}$ 增大 → 容量↑。
- ⚠️ 树模型仍会**过拟合时间序列**——必须用 walk-forward（§14）切分，不能用随机 K-fold。

### 13.4 神经网络（高阶）
- **DNN / CNN / RNN-LSTM / Transformer**——处理序列、跨资产、文本（新闻 / 财报）。
- 容量极大，但 QR 里**过拟合与不可解释**问题最严重，样本量门槛最高。
- 变量敏感性：层数 / 参数量↑ → 容量↑ → 过拟合风险↑；正则化（dropout / weight decay）↑ → 更平滑。

### 13.5 聚类（无监督）
- **K-means / 层次聚类**：对股票按特征聚类，可替代 / 补充行业分类（§6 行业因子）；也可对因子收益聚类找相关性板块。
- 变量敏感性：簇数 $K$ 增大 → 每个簇更小更纯、但可能过细分；距离度量选错 → 聚类无意义。

### 13.6 集成与 Stacking
- 把多个弱模型（不同算法 / 特征 / 时间窗）组合，**降低单一模型方差**、提升稳健。
- 直觉：市场非平稳，没哪个模型永远对，集成比"押单一模型"更稳。

### 13.7 降维与可视化（接 §9）
- PCA（§9）已讲；此外 **t-SNE / UMAP** 用于把高维因子空间投影到 2D 看聚类结构（仅可视化，不可用于特征工程喂模型，因不可逆）。

### 13.8 实战直觉与 ⚠️ 注意
- **先线性、后复杂**：永远先跑 OLS / Ridge 当 baseline，再上树 / 网络。若复杂模型没显著赢 baseline，说明数据里没有非线性结构，别硬上。
- **特征工程 > 模型选择**：QR 里 80% 的效果来自干净的 point-in-time 特征（§COLLABORATION_PLAN 2.2），模型只是放大器。
- **过拟合护栏**：walk-forward（§14）+ 样本外 + Deflated Sharpe（§11.5）+ 多重检验（§11.4）缺一不可。
- **可解释性**：监管 / 实盘要求你能解释"模型为什么买它"；纯黑箱网络在 QR 落地阻力大。

---

## 14. 时间序列（Time Series）

QR 里几乎所有数据都是**随时间演化的序列**：价格、收益、因子值、因子收益 $f$。时间序列分析与横截面（§6、§9 里的"同一时点跨股票"）是两套不同逻辑——时序关注"一个点怎么依赖它的过去"，且金融序列**几乎从不平稳、几乎从独立**，所以方法学完全不同。

### 14.1 为什么 QR 离不开时序分析
- 收益 $r_t$ 不是独立同分布（i.i.d.）：今天涨跌会影响明天（短期反转 / 动量），波动会聚集（§14.5）。
- 横截面因子（§6）需要**逐期**更新暴露 $B$，这本身就是时序过程。
- 回测 / walk-forward（§14.7）本质是在时间轴上切分训练与测试。

### 14.2 平稳性（Stationarity）与单位根
- **平稳**：统计性质（均值、方差、自协方差）不随时间变。只有平稳序列才能放心做回归 / 预测，否则会出现**伪回归**（两个无关的随机游走看起来高度相关）。
- **单位根（Unit Root）**：序列像随机游走 $P_t = P_{t-1} + \varepsilon_t$，非平稳、方差随时间发散。检验用 **ADF 检验**（Augmented Dickey-Fuller），原假设"有单位根 = 非平稳"。
- **处理**：对非平稳序列做**差分** $\Delta P_t = P_t - P_{t-1}$（或对数差分得收益），使其平稳——这正是 ARIMA 里 $d$ 的含义。
- 变量敏感性：
  - 差分阶数 $d$ 增大 → 序列更平稳，但丢失更多长期信息、噪声被放大；
  - **未差分直接回归两个随机游走** → 伪回归，$R^2$ 虚高、t 值不可信（直接踩 §11 的显著性陷阱）。

### 14.3 自相关：ACF / PACF
- **ACF（自相关函数）**：$\rho_k = \text{Corr}(r_t, r_{t-k})$，看滞后 $k$ 期还相关多少。
- **PACF（偏自相关）**：剔除中间滞后影响后，$r_t$ 与 $r_{t-k}$ 的纯相关——用于定 AR 阶数 $p$。
- **白噪声检验**：若所有 $\rho_k \approx 0$，序列无预测价值（Ljung-Box 检验）。
- 变量敏感性：滞后阶 $k$ 增大 → 一般 $\rho_k$ 衰减（金融收益常快速衰减到 0，说明短期记忆弱）。

### 14.4 ARIMA 模型
$(p,d,q)$ 三参数：
- $p$：自回归阶数（用过去 $p$ 期自身预测）；
- $d$：差分次数（使平稳，§14.2）；
- $q$：移动平均阶数（用过去 $q$ 期预测误差）。
- 变量敏感性：
  - $p$ 增大 → 模型记更长历史 → 拟合↑但易过拟合；
  - $d$ 增大 → 更平稳但序列更短、更易噪声化；
  - $q$ 增大 → 对冲击响应更平滑持久。
- ⚠️ 金融日频收益常近似白噪声，纯 ARIMA 预测能力有限；ARIMA 在**波动率**和**慢变量**（利率、估值）上更有用。

### 14.5 波动率聚类与 GARCH（QR 风险核心）
- **波动率聚类（Volatility Clustering）**：大波动后跟大波动、小波动后跟小波动（"风险聚集"）。
- **异方差（Heteroskedasticity）**：方差随时间变，违背 OLS 同方差假设。
- **ARCH / GARCH 模型**：$\sigma_t^2 = \omega + \sum_i \alpha_i \varepsilon_{t-i}^2 + \sum_j \beta_j \sigma_{t-j}^2$——用"过去平方残差 + 过去方差"预测今天方差。GARCH(1,1) 是业界基准。
- 变量敏感性：
  - $\alpha$（冲击项）增大 → 波动对新闻更敏感、聚类更陡；
  - $\beta$（持续项）增大 → 波动衰减更慢、更持久；
  - $\alpha+\beta \to 1$ → 波动近乎"长记忆"（危机后久久不散）。
- 衔接：GARCH 估出的**条件方差**可直接喂 §12 优化器当**时变 $\Sigma$**，或用于风险价值 VaR。

### 14.6 季节性与日历效应
- **季节性**：规律性的周期波动（周内效应、月内效应、一月效应）。
- QR 注意：**日历效应**（如"一月买入""周一效应"）常被因子模型吸收，单独交易易被交易成本吃掉（§8.6）；且许多历史日历效应在样本外已衰减（见指南的因子衰减）。
- 变量敏感性：季节性强度↑ → 周期项权重↑，但过拟合历史周期的风险↑。

### 14.7 滚动窗口与 Walk-forward（回测基石）
- **滚动窗口（Rolling Window）**：用 $[t-w,\, t-1]$ 训练、$t$ 测试，逐期推进，避免用未来数据（前视泄漏，§12.6）。
- **Walk-forward**：训练→测试→再训练→再测试交替，比单次切分更贴近实盘"持续学习"。这是 §11 / §12 反复强调的**样本外验证载体**。
- 变量敏感性：窗口长度 $w$ 增大 → 估计更稳但更慢适应新机制（非平稳下滞后）；$w$ 减小 → 更灵敏但噪声大、易过拟合。

### 14.8 时序交叉验证 vs 随机 K-fold
- **随机 K-fold**（打乱样本）在时序上**等于偷看未来**——某样本的"特征"可能泄漏到它未来期的标签里。
- 时序必须用 **TimeSeriesSplit / walk-forward**（§14.7），严格按时间先后切。这是 ML（§13.3）在金融里最易踩的坑。

### 14.9 实战直觉与 ⚠️ 注意
- **非平稳是常态**：别假设均值 / 方差恒定；用滚动统计（moving mean / vol）而非全样本统计。
- **结构突变（Regime Shift）**：政策、危机、流动性事件会让旧规律失效——walk-forward + 监控（见指南主链路）是护栏。
- **幸存者偏差**（§13.1）：退市股票的历史不在数据里，时序统计会偏乐观。
- **频率选择**：高频噪声大、低频信号少；因子常用日频 / 周频，GARCH 用日频，宏观用月频。

---

## 15. 因子预处理：winsorize / 中性化 / 正交化

造因子暴露矩阵 $B$（或你项目里的 $X$）之前，原始因子值（市值、PE、过去收益等）几乎从不能直接进矩阵——它们有极端值、量纲不同、还会暗含行业 / 市值等不想要的敞口。这一节讲三步标准流水线：winsorize（治极端值）→ 标准化（§5 z-score）→ 中性化（去不想要的风险）→ 正交化（去因子间冗余）。这三步直接决定 $B$ 干不干净、满不满秩、优化器稳不稳（§12.5 条件数、§9 退化子空间）。

### 15.1 为什么必须预处理（不做的后果）

- **极端值主导**：一个 PE 异常的股票会把整列方差撑爆，PCA 第一主成分（§9.5）和 OLS 系数（§4.2）被它带歪。
- **量纲混在一起**：市值是 $10^{11}$ 量级、动量收益是 $10^{-2}$ 量级，直接并排进 $B$ 列 → 大数值因子天然压过小数值因子（同 §9.3 不标准化 PCA 的后果）。所以标准化（§5）是强制的。
- **暗含结构性敞口**：一个"价值"因子可能只是偷偷押了小市值（小票 PE 低），没做中性化就会和规模因子撞车（§7 伪相关）。

### 15.2 Winsorize（缩尾）

把超出分位阈值的极端值"剪"到阈值上，而不是删掉：

$$x_i' = \max\big(\min(x_i,\; P_{u}),\; P_{l}\big)$$

常用 $P_l = 1\%$、$P_u = 99\%$（或 2.5% / 97.5%）。$P_u$ 是第 99 百分位，$x_i$ 超过它就被压成 $P_u$。

**变量增大/减小 → 指标怎么变 → 为什么**
- 缩尾阈值**收紧**（$5\%/95\%$ 比 $1\%/99\%$ 更紧）→ 被截断的值**更多** → 极端值影响**更小**，但可能损失尾部信息、引入人为截断偏差。
  - 为什么：更多点被拉到边界，极端值不再单独撑方差。
- 缩尾阈值**放宽**（如 $0.1\%/99.9\%$）→ 更接近原始数据、保留尾部，但极端值仍可能主导。
- **完全不做 winsorize** → 单只异常股主导 PCA / 回归 → $B$ 估计失真。

### 15.3 中性化（Neutralization）：去掉不想要的风险暴露

核心思想：把因子值对一组"控制变量"（行业哑变量、$\log$ 市值、beta 等）做 OLS 回归，取**残差**——残差就是"因子中无法被这些控制变量解释的那部分"，即纯净信号。

$$f_i^{\text{neu}} = f_i - \hat f_i, \qquad \hat f_i = \text{OLS}\big(f_i \sim \text{Industry}_i + \log(\text{MktCap}_i) + \beta_i\big)$$

- **行业中性**：对行业哑变量回归取残差 → 因子不再悄悄押注某个行业。这正是 §12.3 优化约束 $B_{\text{ind}}^\top w = 0$ 的**因子层面版本**——一个在构造期就中性，一个在优化期再约束，殊途同归。
- **市值 / 风格中性**：对 $\log(\text{MktCap})$、beta 回归取残差 → 剥离规模 / 系统性风险敞口。

**变量增大/减小 → 指标怎么变 → 为什么**
- 控制变量**加多** → 残差越"纯净"（去偏越多）→ 但也可能把真实信号一并回归掉（**过中性化**）。
  - 为什么：回归能解释的都被拿走了，包括一部分本该属于因子的有效信息。
- **不做中性化** → 因子暗含行业 / 市值敞口 → 优化时（§12）无意中重仓这些维度；或因子 IC 是假象（其实被规模 / 行业驱动，见 §7 伪相关）。
- 中性化后因子 IC（§8.7）通常**下降但更稳健** → 因为去掉了"白送的"结构暴露，剩下的是真预测力。

### 15.4 正交化（Orthogonalization）：消除因子间相关

造好一批候选因子后，它们彼此常高度相关（§3.2）。相关列并进 $B$ → 矩阵病态（§12.5 条件数爆炸、§9 退化子空间）→ 优化器权重失控。正交化用 Gram-Schmidt 式投影，把每个因子对"前面因子的残差空间"投影，使结果因子彼此不相关：

$$f^{(k)}_{\perp} = f^{(k)} - \sum_{j<k} \frac{\text{Cov}(f^{(k)},\, f^{(j)}_{\perp})}{\text{Var}(f^{(j)}_{\perp})}\, f^{(j)}_{\perp}$$

- 直觉：第 $k$ 个因子只保留"前 $k-1$ 个都解释不了的新信息"。结果与 PCA 主成分（§9.4）一样彼此正交，但**保留经济含义顺序**（先放重要因子）。
- 用途：去冗余（相关因子重复计价同一风险源）、让 $B$ 列正交 → 优化更稳、风险归因更干净。

**变量增大/减小 → 指标怎么变 → 为什么**
- 正交化**强度↑（完全正交）** → $B$ 条件数改善、优化更稳 → 但因子经济含义被扭曲（正交后的"价值"不再是原始价值，是除掉动量后的残差），可解释性↓。
  - 为什么：投影把因子间的共有信息全切走了。
- **部分正交化**（只去行业 / 市值，因子间保留相关）→ 平衡可解释性与数值稳定性，实务更常用。

### 15.5 实战串联（接 §6 / §9 / §12）

典型流水线：原始因子 → **winsorize**（§15.2）→ **z-score 标准化**（§5）→ **中性化**（行业 / 市值，§15.3）→ **正交化**（可选，§15.4）→ 进 $B$。每一步都在服务同一个目标：**让 $B$ 干净、满秩、可解释**。你 `COLLABORATION_PLAN` 里的 `build_factor_exposure` / `validate_factors` 就是这套流程的落地。

---

## 16. 风险度量：VaR / CVaR（尾部风险）

§8 的波动率 $\sigma$ 是"平均波动幅度"，但投资者真正怕的是"最坏会亏多少"。VaR / CVaR 直接度量**尾部损失**，是 §12 优化、§14.5 GARCH 的下游产出，也是实盘风控 KPI。

### 16.1 VaR（Value at Risk，在险价值）

定义：在置信水平 $c$（如 95%）和持有期 $\Delta t$ 下，组合**最大可能损失（不被超越的概率 = $c$）**：

$$P(R_p \le -\text{VaR}_c) = 1-c$$

即"有 $c$ 概率，这段时间的损失不会超过 VaR$_c$"。三种算法：
1. **历史模拟法**：直接取历史收益的经验分位数，不假设分布。$\text{VaR}_{95} =$ 历史收益 5% 分位数。
2. **参数法（方差-协方差 / delta-normal）**：$\text{VaR}_c = z_c \cdot \sigma_p \cdot \sqrt{\Delta t}$，$z_c$ 为正态分位数（95%→1.645，99%→2.326）。直接复用 §2/§3 的 $\sigma$ 和 §12 的 $\Sigma$。
3. **蒙特卡洛法**：模拟大量收益路径取分位数（衔接 §随机过程）。

**变量增大/减小 → 指标怎么变 → 为什么**
- 置信水平 $c$ **↑** → VaR **↑**（要求覆盖更极端情形）。
- 波动率 $\sigma_p$ **↑** → VaR **↑**。
- 持有期 $\Delta t$ **↑** → VaR **↑**（按 $\sqrt{\Delta t}$ 缩放，同 §8.8 年化逻辑）。
- 收益**肥尾**（真实 vs 正态，见 §7）→ 参数法 VaR **低估**尾部 → 历史 / 蒙特卡洛更稳。

### 16.2 CVaR / Expected Shortfall（条件 VaR，更狠）

VaR 的软肋：只承诺"95% 不会超过 X"，却不说"超过了会亏多惨"。CVaR 取**超过 VaR 那部分损失的平均值**：

$$\text{CVaR}_c = E\!\left[\,R_p \mid R_p \le -\text{VaR}_c\,\right]$$

- **为什么更优**：满足**次可加性（coherent risk measure）**——组合 CVaR ≤ 各部分 CVaR 之和，天然鼓励分散化；VaR 不满足（可能导致反直觉的集中）。
- **衔接 §12**：CVaR 可直接当优化目标（min-CVaR 组合），比 min-variance 更针对尾部。
- **变量增大/减小 → 指标怎么变 → 为什么**
  - 收益**肥尾越厚** → CVaR 比 VaR **高得越多**（超限损失被极端值拉高）。
  - 置信 $c$ **↑** → CVaR **↑**（看更深的尾）。

### 16.3 局限与衔接

- VaR **不具次可加性** → 不能用 VaR 直接做组合优化（会得出反直觉结果），CVaR 可以。
- 都依赖波动估计 → 回到 §10 Ledoit-Wolf（噪声让 VaR 失真）、§14.5 GARCH（时变 $\sigma$ 让 VaR 动态更新）。
- 与 §8 互补：VaR 是"尾部损失金额"，Sharpe 是"风险调整后效率"，一个看防守、一个看进攻。
- **压力测试（stress test）** 补充：人为设定极端情景（如 §9.4"第一主成分跌 3σ"）算损失，是 VaR 之外的另一道防线。

---

## 17. 因子衰减 / IC 时序衰减

因子不是永恒有效的。一个今天 IC=0.08 的因子，三年后可能趋近 0——这就是**因子衰减（factor decay）**。理解它，才能把 §11 假设检验、§14 非平稳、§13 ML 的持续训练串成闭环。

### 17.1 现象与统计本质

因子 IC 是一条时间序列 $\{\text{IC}_t\}$（§8.7、§11.3）。衰减表现为：
- IC **时序均值 $\overline{\text{IC}}$ 随时间下降**（预测力被套利走 / regime shift，§14.9）。
- IC **时序波动 std(IC) 上升或不变** → $\text{ICIR} = \overline{\text{IC}}/\text{std}(\text{IC})$ **下降**（§11.3）。
- 进而 $t_{\text{IC}} = \text{ICIR}\cdot\sqrt{T}$ 有效性递减 → 假设检验从显著变不显著（§11）。

一句话：**样本内显著 ≠ 样本外有效，衰减是根因之一。**

### 17.2 怎么度量衰减

- **半衰期（half-life）**：IC 均值降到峰值一半所需时间。常用指数拟合 $\overline{\text{IC}}(t) = a\,e^{-t/\tau} + b$，$\tau$ 即衰减时间常数；$\tau$ 越小衰减越快。
- **滚动 IC**（§14.7）：用滚动窗口算 IC 画曲线，看趋势——比全样本单点 IC 更诚实（避免被长期平均掩盖近期塌方）。
- **分层回测衰减**：按因子分组的多空收益，在不同子 period 分别看，检查单调性是否随时间变弱。

**变量增大/减小 → 指标怎么变 → 为什么**
- 市场有效性 **↑**（更多资金套利该因子）→ IC 均值 **↓** → 衰减 **↑**。
- 样本外越**长**（§11 多重检验 / walk-forward）→ 越容易暴露衰减（样本内漂亮、样本外塌）。
- 因子越**拥挤（crowding）** → 衰减越**快**。

### 17.3 与前面章节的闭环

- 接 **§11 假设检验**：衰减让样本内显著在样本外失效 → 必须 walk-forward（§14.7）+ 样本外再验。
- 接 **§14 非平稳 / regime shift**：衰减就是"规律会变"的具体表现。
- 接 **§13 ML**：非平稳 + 衰减 → 模型要持续再训练，不能一次训完吃终身。
- **实战**：因子库要**定期重估 IC / 重新 `validate_factors`（§6.3、COLLABORATION_PLAN）**，衰减超阈值的因子降级或剔除——这是监控主链路（指南）的量化依据。

---

## 18. 随机过程（Stochastic Processes）

前面 §2/§3 把收益当成"有均值和方差的随机变量"、§14 把序列当成"有自相关和时变波动的过程"——但都没讲**这些随机量随时间怎么演化**。随机过程就是给"不确定性随时间展开"建立数学模型：它是价格模拟、期权定价、蒙特卡洛风险（§16.1）的理论地基，也解释了 §14.2 单位根随机游走为何非平稳。

### 18.1 为什么 QR 需要随机过程

- **描述价格动态**：股价不是静态分布，而是一条会随机游走、会跳跃的路径；要模拟它、给它定价、算极端情景，得先有"它怎么随机动"的模型。
- **给前面的统计量一个动态故事**：§2 的方差、§3 的协方差、§14.5 的 GARCH 波动率，本质上都是在刻画随机过程里的"σ"；随机过程告诉我们 σ 怎么随路径累积。
- **蒙特卡洛 VaR / CVaR**（§16.1）和**压力测试**（§16.3）都依赖"先假设一个随机过程、再抽大量路径"——没有它，尾部风险只能靠历史数据硬估。

### 18.2 随机游走（Random Walk）

最朴素的模型：每期价格在上期基础上加一个随机扰动。

$$P_t = P_{t-1} + \varepsilon_t, \qquad \varepsilon_t \stackrel{\text{i.i.d.}}{\sim} (0,\sigma^2)$$

等价地，对**对数价格**做随机游走：$\ln P_t = \ln P_{t-1} + \eta_t$（$\eta_t$ 为随机收益）。

- **关键性质：方差随时间线性增长**。累积 $t$ 期后 $\text{Var}(P_t) = t\sigma^2$——这正是 §14.2 说的**单位根 / 非平稳**：价格水平方差发散，不能直接做回归。
- **有效市场假说的极端版**：若价格严格随机游走，则"明天的涨跌"与过去无关、不可预测——这也是为什么纯技术预测难（但 §14.1 的短期反转 / 动量说明真实市场只是"近似"随机游走）。

**变量增大/减小 → 指标怎么变 → 为什么**
- 步长方差 $\sigma^2$ **↑** → 路径离散度 **↑**（$\text{Var}(P_t)=t\sigma^2$）→ 越难预测、回测越易过拟合噪声。
  - 为什么：每步扰动更大，长期累积出的路径更发散。
- 时间跨度 $t$ **↑** → 方差 **↑**（线性）→ 长期价格越不可预测。
- 对**收益**而非价格做随机游走 → 收益是平稳白噪声（§14.3），可直接建模；对价格做 → 非平稳，须先差分取收益（§14.2）。

### 18.3 维纳过程 / 布朗运动（Wiener Process）

把离散随机游走放到**连续时间**取极限，就得到维纳过程 $W_t$（布朗运动）：

$$dW_t \sim \mathcal{N}(0, dt), \qquad W_0 = 0$$

性质：
- **独立增量**：$W_t - W_s$ 只依赖 $(t-s)$，且与过去无关（马尔可夫，§18.5）。
- **分布**：$W_t - W_s \sim \mathcal{N}(0, t-s)$——和 §18.2 的"方差随 $\Delta t$ 线性增长"完全对应。
- **路径连续但处处不可微**：价格在任意小区间都在抖，所以不能用普通微积分，得用 §18.6 伊藤引理。

直觉：维纳过程就是随机过程世界的"噪声原子"——后面所有带随机性的模型（GBM、期权、利率）都是它加料而来。

### 18.4 几何布朗运动 GBM（价格模型）

给维纳过程加一个"漂移项"和"比例于价格的波动"，得到最常用的股价模型：

$$dS_t = \mu S_t\, dt + \sigma S_t\, dW_t$$

写成对数形式更清楚：

$$d(\ln S_t) = \left(\mu - \frac{\sigma^2}{2}\right) dt + \sigma\, dW_t$$

解析解：$S_t = S_0 \exp\!\big((\mu - \sigma^2/2)t + \sigma W_t\big)$。

- **为什么用"几何"**：价格不能为负，对数收益正态 → 价格对数正态（log-normal），符合 §14 用对数收益的习惯。
- **漂移修正 $\mu - \sigma^2/2$**：波动越大，对数漂移反而要减去一项——这是**对数凹性（Jensen 不等式）**的结果：$E[\ln S_T] \neq \ln E[S_T]$。高波动会拉低"中位数路径"。
- 参数：$\mu$ = 期望漂移（收益），$\sigma$ = 波动率。

**变量增大/减小 → 指标怎么变 → 为什么**
- 漂移 $\mu$ **↑** → 价格路径整体**上移**（中位路径更高）。
- 波动率 $\sigma$ **↑** → 路径离散度**指数级放大**（指数里的 $\sigma W_t$），且对数漂移被多减 $\sigma^2/2$ → 中位数路径**下移**。
  - 为什么：波动越大，极端上行和下行都更猛；但凹性让"平均对数"被波动率拖累。
- 时间 $t$ **↑** → 离散度 **↑**（指数项方差 $\propto \sigma^2 t$）。

### 18.5 马尔可夫性质（Markov Property）

若一个过程"未来只依赖现在、与过去无关"，即

$$P(S_{t+1} \mid S_t, S_{t-1}, \dots) = P(S_{t+1} \mid S_t)$$

则称它具有马尔可夫性。

- **为什么 QR 关心**：弱式有效市场下，价格近似马尔可夫——你只需要当前价格和已知信息，不必翻历史。这给很多模型（随机游走、GBM、期权定价）提供了合法性。
- **但波动率不马尔可夫**：§14.5 的波动聚集说明，今天波动大 → 未来波动也大，波动率本身有"记忆"（需 GARCH 状态变量）。所以"价格近似马尔可夫、波动率非马尔可夫"是实务里的精细区分。
- 衔接 §14：AR(1) 是马尔可夫过程；但带长记忆的序列（§14.5 $\alpha+\beta\to1$ 的 GARCH）违背纯马尔可夫。

### 18.6 伊藤引理（Itô's Lemma）

普通微积分里，函数 $f(S_t)$ 的微分用链式法则；但当 $S_t$ 是随机过程（含 $dW_t$）时，普通链式法则**会漏掉一项**。伊藤引理给出正确形式：

$$df = \left(\frac{\partial f}{\partial t} + \mu S \frac{\partial f}{\partial S} + \frac{1}{2}\sigma^2 S^2 \frac{\partial^2 f}{\partial S^2}\right) dt + \sigma S \frac{\partial f}{\partial S}\, dW_t$$

- **多出 $\frac{1}{2}\sigma^2 S^2 f_{SS}$ 的原因**：随机微积分里 $dW_t^2 = dt$（二次变分不为零），而普通微积分 $(dt)^2=0$。这一项就是"波动的二阶效应"。
- **为什么 QR 关心**：
  - **期权定价**：Black-Scholes 方程本质就是对期权 payoff 应用伊藤引理；
  - **解释 §18.4 的修正项**：对数 $\ln S_t$ 的二阶导是 $-1/S^2$，代入伊藤引理正好得到漂移里的 $-\sigma^2/2$；
  - 凸性资产（期权）对波动更敏感，正是这个二阶项在起作用。

**变量敏感性**
- 波动率 $\sigma$ **↑** → 二阶修正项 $\frac{1}{2}\sigma^2 S^2 f_{SS}$ **↑** → 凸性（如期权 gamma）越重要的资产，波动影响越大。
- 函数越凸（$f_{SS}$ 越大，如深度虚值期权）→ 伊藤二阶项越不可忽略。

### 18.7 蒙特卡洛模拟（Monte Carlo）

把随机过程离散化，抽大量随机路径，再用经验分布回答"最坏情况 / 定价多少 / 风险多大"。GBM 的离散化步进：

$$S_{t+\Delta t} = S_t \exp\!\left(\left(\mu - \frac{\sigma^2}{2}\right)\Delta t + \sigma \sqrt{\Delta t}\, Z\right), \qquad Z \sim \mathcal{N}(0,1)$$

每抽一个 $Z$ 就走出一条路径，抽 $N$ 条后看终值分布。

**变量增大/减小 → 指标怎么变 → 为什么**
- 模拟路径数 $N$ **↑** → 估计越接近真实分布（蒙特卡洛误差 $\propto 1/\sqrt{N}$）→ 但算力成本 **↑**。
  - 为什么：大数定律，样本越多均值越稳。
- 时间步长 $\Delta t$ **↓**（更细）→ 离散化误差 **↓**、路径更准 → 但步数变多、更慢。
- 波动率 $\sigma$ **↑** → 路径终值分布 **更宽** → VaR / 期权价格更分散（衔接 §16.1 蒙特卡洛法）。

**用途**：蒙特卡洛 VaR / CVaR（§16.1）、期权定价、组合压力测试（§16.3）、以及"如果市场按 GBM 演化，我的组合会怎样"的情景推演。

### 18.8 实战直觉与 ⚠️ 注意

- **GBM 是基线不是真理**：它假设 $\mu,\sigma$ 恒定、收益对数正态——但真实收益有**肥尾**（§7）、**随机波动率**（§14.5）、**跳跃（jumps）**。所以 GBM 常用于定价和基线风险，但预测 alpha 得靠 §6 因子 / §13 ML。
- **随机游走有效性有争议**：动量、短期反转（§14.1）说明价格只是"近似"随机游走，纯 RW 假设会低估可预测性（也会高估，取决于你信哪边）。
- **和前面章节的闭环**：§2 方差、§3 协方差、§14 GARCH 的"σ"在这里有了动态含义；§16 的蒙特卡洛 VaR、§12 的风险模拟都建立在本节模型上。理解随机过程，你就知道"那些统计量到底在描述一条怎样随机演化的路径"。

---

## 19. 对数收益 vs 简单收益（收益的表达方式）

### 19.1 为什么必须分清
前面 §8.8 提过"复利陷阱"、§18.4 用了对数形式 `ln(S_t/S_{t-1})` 推 GBM——但**从没正式定义两种收益**。QR 内部几乎全用一种、对外展示用另一种，混用是回测里最常见的"隐形 bug"。本节把底座钉死。

### 19.2 两个定义
- **简单收益（simple / arithmetic return）**：
  $$r_t^{\text{simple}}=\frac{P_t-P_{t-1}}{P_{t-1}}=\frac{P_t}{P_{t-1}}-1$$
- **对数收益（log / continuously-compounded return）**：
  $$r_t^{\log}=\ln\frac{P_t}{P_{t-1}}=\ln(1+r_t^{\text{simple}})$$

两者关系：`r_log = ln(1 + r_simple)`，反过来 `r_simple = exp(r_log) − 1`。

### 19.3 核心区别（为什么 QR 内部偏爱 log）

| 性质 | 简单收益 | 对数收益 |
|---|---|---|
| **跨期可加性** | ❌ 不可加，要连乘 `∏(1+r)` | ✅ 可加 `r_log[t1,t3]=r_log[t1,t2]+r_log[t2,t3]` |
| **对称性** | ❌ 涨 a% 再跌 a% 不回原点（亏更多） | ✅ 近似对称，回到原点附近 |
| **组合聚合** | ❌ 组合收益 ≠ 权重×个股收益 | ✅ 权重近似不变时，组合 log≈∑wᵢ·logᵢ |
| **统计友好** | 受复合效应扭曲 | 近似正态、可直接回归/PCA/方差分解（§3/§9） |

### 19.4 一个关键近似（衔接 §18.4）
当 `|r|` 很小（日频常见），泰勒展开给：
$$r_t^{\log}\approx r_t^{\text{simple}}-\tfrac12(r_t^{\text{simple}})^2$$
即**对数收益略小于简单收益**，差的那半次项正是 §18.4 GBM 漂移里的 `−σ²/2`（Jensen 不等式，连续复利的对数漂移被方差吃掉一块）。这也是 §8.8 "年化复利陷阱"的数学来源。

### 19.5 怎么选（实战规则）
- **内部计算全用 log**：因子构建、回归、协方差、PCA、风险模型都建立在 log 的可加/正态性上。
- **对外展示用 simple 或年化 simple**：投资组合业绩、给人的"涨了百分之几"必须用 simple（业务语义）。
- **多期累计**：用 log 求和 → `exp(Σr_log)` 回价格；用 simple 必须连乘 `(1+r)` 链。
- 变量敏感性：
  - 波动 `σ ↑` → log 与 simple 的差距 `½σ²` ↑；**日频差≈0，月/年频明显**。
  - 单期幅度 `|r| ↑` → 两者差距非线性放大（近似项权重上升）。

### 19.6 ⚠️ 常见错误
- **混合使用**：一部分信号用 log、一部分用 simple，回测结果无意义（§17 会误判成"因子有效"）。
- **价格含 0/负**：杠杆产品、收益率序列本身可能非正，log 不适用，需换口径。
- 衔接 §18.4：GBM 用 `d ln S` 是天然选择，因为对数收益 additive、正态增量。

---

## 20. 数据三大陷阱：前视 / 幸存者 / 时点数据（QR 生死线）

### 20.1 为什么这是生死线
所有因子、IC、回测都建立在一个隐含假设上：**"回测当下，你只能用到那时已公开的信息"**。踩中任意一条，样本内漂亮、样本外崩盘——而且你往往分不清是"因子真衰减"（§17）还是"数据在造假"。**你 `data_io.py` 数据清洗的第一职责，就是保证每个 (股票, 时间) 单元格只含当时已知信息。**

### 20.2 前视偏差（Look-ahead Bias）
- **定义**：用了回测时点"还不可能知道"的信息。
- **典型场景**：
  - 用**当天收盘价**算信号，却在同一天以收盘价成交（实际至少要 T+1 开盘/收盘）。
  - 用季度财报的"报告日"数据，却按"财报期结束日"对齐（中间有披露延迟）。
  - 因子值引用了未来才发生的公司行动（分红、拆合、停牌）。
- **为什么致命**：信息从未来"泄漏"到现在，IC 虚高、夏普虚高。
- **防护**：信号 t 只用 t 收盘前可得数据；成交价用 t+1 开盘/收盘；公司行动按当时**公告日**取，不用当前数据库快照。
- 变量敏感性：泄漏窗口越长（如直接用了次日数据）→ IC 虚高越多 → 样本外崩得越狠。

### 20.3 幸存者偏差（Survivorship Bias）
- **定义**：只研究"现在还活着"的股票，剔除已退市/合并/破产的。
- **典型场景**：用**当前指数成分**回测 10 年前——等于你 10 年前就预知谁会留下；只下载"至今仍在市"的股票历史。
- **为什么致命**：留下的偏向"涨得好/没崩"的，因子均值被拉高、风险被低估，对小市值/高波动因子尤其严重。
- **防护**：用**全历史宇宙**（含退市），或用 point-in-time 指数成分；回测时按当时可得的成分，不是现在的。
- 变量敏感性：退市率越高（如小盘股、港股创业板）→ 偏差越大。

### 20.4 时点数据（Point-in-Time, PIT）
- **定义**：每个时刻只用"该时刻真实可得"的数据**版本**，而非事后被公司修订/重述的版本。财报会重述、因子值会被回写数据库。
- **典型场景**：用当前数据库里的 EPS（已被公司重述）回测 5 年前，等于偷看了未来的修订；用"最新总市值"对齐历史信号。
- **为什么致命**：重述往往发生在公司出事之后，用重述值等于把"坏消息已发生"泄漏进过去。
- **防护**：快照/事件表按**首次披露日**取数；财务与因子用"当时公告"版本，不用事后修订版。
- 衔接 §15：winsorize / 中性化也要用 PIT 数据，否则缩尾阈值、中性化残差本身也含未来信息。

### 20.5 三者叠加 + 回测前自查清单
三者常常**叠加**：非 PIT 数据 + 幸存者宇宙 + 当天成交 = 三重未来泄漏，IC 可以高到离谱。
> **回测前可勾选清单**：
> 1. ☐ 成交价格是否在信号之后（至少 T+1）？
> 2. ☐ 股票宇宙是否含已退市标的？
> 3. ☐ 财务/因子是否用首次披露版本（非重述）？
> 4. ☐ 公司行动（分红/拆合/停牌）是否按公告日对齐？
> 5. ☐ 训练/验证是否严格按时间切（见 §14.7 walk-forward，禁止随机 K-fold 混时空）？

### 20.6 实战直觉
数据清洗的优先级**高于任何因子创新**。一个 IC=0.05 但干净的数据，远胜一个 IC=0.12 但含前视的数据——后者在实盘（§17 衰减 + 真实成本 §8.6）会归零甚至变负。

---

## 21. 对照术语表补遗（Glossary）

收尾整理：把前文**未单独定义**、但 QR 交流/代码里高频出现的术语集中对照。已在他处详述的，标注见章节。

| 术语 | 英文 | 一句话定义 | 详见 |
|---|---|---|---|
| 多空组合 | Long-Short | 做多高分股票、做空低分股票，对冲市场/行业暴露，净头寸≈0 | §19 收尾延伸、组合构建（待 §P1） |
| 市场中性 | Market-Neutral | 组合对市场风险（beta）近似零暴露，收益来自选股而非大盘方向 | §15.3 中性化、§20 |
| 纸面交易 | Paper Trading | 不投入真实资金的模拟交易，验证信号与管道，不上实盘 | — |
| 滑点 | Slippage | 预期成交价与实际成交价的差（含流动性、延迟） | §8.6、§20.2 |
| 市场冲击 | Market Impact | 大单自身买卖压低/抬高价格的成本，随下单量↑而↑ | §8.6、§20 |
| 买卖价差 | Bid-Ask Spread | 买价与卖价之差，最小交易成本下限 | §8.6 |
| 时点数据 | Point-in-Time (PIT) | 只用当时可得的数据版本，不含未来修订 | §20.4 |
| 前视偏差 | Look-ahead Bias | 回测中用了未来信息 | §20.2 |
| 幸存者偏差 | Survivorship Bias | 只研究存活标的导致偏差 | §20.3 |
| 协整 | Cointegration | 两非平稳序列的线性组合平稳，可配对交易 | §27 |
| 凯利仓位 | Kelly Criterion | 在已知胜率/赔率下最大化对数增长的最优下注比例 | §26 |
| 指数加权协方差 | EWMA | 近期数据权重更高的协方差估计，另一种去噪 | §24 |
| Bootstrap | Bootstrap | 有放回重采样估计统计量置信区间（如 IC/Sharpe） | §25 |
| 偏度 / 峰度 | Skewness / Kurtosis | 分布的三/四阶矩，刻画不对称与肥尾 | §22 |
| Cornish-Fisher VaR | — | 用偏度/峰度修正正态假设的分位 VaR | §23 |
| Fama-MacBeth | — | 两阶段截面回归检验因子溢价的经典方法 | §28 |

> 标注"待补（§P1/§P2）"的术语现已补为正文章节：Fama-MacBeth→§28、多空/市场中性→§29、偏度/峰度→§22、肥尾/Cornish-Fisher VaR→§23、EWMA→§24、Bootstrap→§25、Kelly→§26、协整→§27；系统性回测方法论见 §30。

---

## 22. 偏度与峰度（高阶矩风险）

### 22.1 为什么均值方差不够
§2/§3 用均值（一阶矩）和方差（二阶矩）描述分布，§7 也提"正态假设下方差够用"。但真实收益**非正态**：涨多跌少不对称（偏度）、暴跌比正态更频繁（峰度）。只用方差会**低估尾部风险**——这正是 §16 VaR 在危机中失守的根因。高阶矩刻画"形状"。

### 22.2 偏度 Skewness（三阶矩）
$$\text{Skew}(r)=E\left[\left(\frac{r-\mu}{\sigma}\right)^3\right]$$
- $\text{Skew}>0$ 右偏：右侧（大涨）尾巴更长——收益分布常见（涨得慢、偶尔大涨）。
- $\text{Skew}<0$ 左偏：左侧（大跌）尾巴更长——**期权空头、卖保险策略典型**，危机时巨亏。
- 变量敏感性：极端负值增多 → Skew↓；样本越小 → 估计越噪（衔接 §11 小样本）。

### 22.3 峰度 Kurtosis（四阶矩）
$$\text{Kurt}(r)=E\left[\left(\frac{r-\mu}{\sigma}\right)^4\right],\quad \text{超额峰度 ExKurt}=\text{Kurt}-3$$
- 正态分布 ExKurt=0；$\text{ExKurt}>0$ 尖峰厚尾（极端事件比正态多，金融收益常态）；$\text{ExKurt}<0$ 平顶薄尾。
- 变量敏感性：肥尾样本（如含崩盘日）→ ExKurt↑；缩尾/EWMA 会压低估计（衔接 §15/§24）。

### 22.4 对组合与风险的意义
- 完整风险需三/四阶矩：组合 coskewness $w^\top M_3 w$、cokurtosis $w^\top M_4 w$。但高阶矩估计**极不稳定**（数据量需求∝矩阶数），实务常退而求其次用 §16 CVaR / §23 Cornish-Fisher。
- 偏度偏好：投资者通常**偏好正偏度**（博小概率大涨）、厌恶负偏度——解释为何卖出虚值看跌（负偏、收保费）长期易爆。
- 衔接 §16：正态 VaR 忽略偏度/峰度 → 危机低估；§23 用 Cornish-Fisher 修正。

### 22.5 ⚠️ 注意
- 高阶矩对异常值极度敏感（4 次方放大），务必先 §15 winsorize / 用 PIT 数据（§20）。
- 样本外高阶矩几乎不可预测（比均值还难），多用于**风险诊断**而非预测。

---

## 23. 肥尾分布、Student-t 与 Cornish-Fisher VaR

### 23.1 为什么正态不够
§7/§16 假设收益正态，但 §22 峰度告诉我们真实收益厚尾。正态下"日跌 5σ"概率≈1e-6（百万年一遇），实际几年就来一次。**用正态 VaR 会系统性低估尾部损失**。

### 23.2 肥尾分布
- 定义：尾部比指数衰减更慢（Pareto 型），极端事件概率显著高于正态。
- 典型：Student-t（自由度 ν 越小尾越厚）、广义极值 GEV、稳定分布（α<2 时方差无穷）。
- 变量敏感性：自由度 ν↓（t 分布）→ 尾部越厚 → 同等置信 VaR 越大；ν→∞ 退化为正态。

### 23.3 Student-t 分布
$$f(x)\propto\left(1+\frac{x^2}{\nu}\right)^{-(\nu+1)/2}$$
- 比正态多一个**自由度 ν** 控制厚尾；ν 小→厚尾，ν 大→接近正态。
- 实务：用历史收益拟合 ν（或固定 ν=3~5 作经验值），再算分位数 VaR。

### 23.4 Cornish-Fisher VaR（非正态修正）
把正态分位数 $z_\alpha$ 用偏度 S、超额峰度 K 修正：
$$z_\alpha^{CF}\approx z_\alpha+\frac{1}{6}(z_\alpha^2-1)S+\frac{1}{24}(z_\alpha^3-3z_\alpha)K-\frac{1}{36}(2z_\alpha^3-5z_\alpha)S^2$$
- VaR_CF = μ + σ·$z_\alpha^{CF}$（衔接 §16.1 参数法框架，仅分位数被修正）。
- 直觉：负偏（S<0）→ 分位数更靠左（损失更大）；厚尾（K>0）→ 分位数更极端。
- 变量敏感性：|S|↑ 或 K↑ → $z_\alpha^{CF}$ 偏离正态越多 → VaR 修正越大（通常变大）。

### 23.5 与前面章节闭环
- 衔接 §16：Cornish-Fisher 是参数法 VaR 的**非正态升级**，仍快但依赖矩估计质量。
- 衔接 §22：直接吃 Skew/Kurt 输入。
- 衔接 §10/§24：若用 Student-t / EWMA 估波动，本身就是厚尾建模。
- 局限：仍假设矩稳定（§14 非平稳下会漂移）；极端尾部（>99.9%）任何参数法都不可靠，得靠 §16 历史法或压力测试。

---

## 24. 指数加权协方差 EWMA

### 24.1 为什么需要另一种协方差
§3/§10 的样本协方差平等对待所有历史日；但市场波动**时变**（§14.5 GARCH）：最近的危机比 3 年前更代表"明天的风险"。EWMA 给近期更高权重，是 Ledoit-Wolf（§10）之外的另一主流去噪/时变估计。

### 24.2 定义
$$\Sigma_t=(1-\lambda)r_{t-1}r_{t-1}^\top+\lambda\Sigma_{t-1}$$
- $\lambda\in(0,1)$ 是遗忘因子，常用 0.94（RiskMetrics）或 0.97。
- 等价递推形式无需存全部历史，适合流式更新。

### 24.3 性质与敏感性
- 有效样本量 $T_{eff}\approx(1-\lambda)^{-1}$：λ=0.94→约 16 天，λ=0.97→约 33 天。λ↑→看得越远越平滑；λ↓→越盯近期。
- 变量敏感性：λ↑ → 协方差更平滑、对单日冲击迟钝、但滞后于 regime 切换；λ↓ → 反应快但噪。
- 危机时：大跌日 $r_{t-1}r_{t-1}^\top$ 被近期加权放大 → 风险及时上升（对比等权样本协方差要等很多天"平均"进去）。

### 24.4 与 Ledoit-Wolf / GARCH 的关系
- vs §10 Ledoit-Wolf：LW 是**横截面收缩**（样本→结构化目标），EWMA 是**时序加权**（近期→远期）。可组合：对 EWMA 估计再 LW 收缩。
- vs §14.5 GARCH：GARCH 建模波动的**随机过程**（均值回复），EWMA 是其特例（IGARCH，β=λ）。
- 衔接 §16.1：EWMA 协方差可直接喂参数/蒙特卡洛 VaR。

### 24.5 ⚠️ 注意
- EWMA 只改权重，不解决"T<N 不可逆"（§9.2/§10），仍需收缩或 PCA 降维。
- λ 是固定超参，不会自动适应 regime；剧烈切换时滞后。

---

## 25. Bootstrap 置信区间

### 25.1 为什么 t 值不够
§11 用解析 t 值/ICIR 判显著性，但依赖正态、独立、大样本假设。真实收益**肥尾+自相关+小样本**（§14/§22），解析标准误失真。Bootstrap 用**数据自身重采样**直接估统计量的不确定性，不假设分布形态。

### 25.2 方法
1. 从原始样本（如 T 期 IC 序列）有放回抽 T 个，得自助样本。
2. 在该样本上重算统计量（如 mean IC、Sharpe、ICIR）。
3. 重复 N 次（如 1000），得统计量经验分布 → 取 2.5%/97.5% 分位为 95% CI。

### 25.3 两种主要变体
- **普通 Bootstrap**：假设观测独立（适合截面/已去趋势的 IC）。
- **Block Bootstrap**：整块（如 20 日）重采样，保留时序相关（§14.3 ACF），用于收益/因子收益序列。
- 变量敏感性：N↑ → CI 越稳（误差∝1/√N，衔接 §18.7）；T↑ → CI 越窄。

### 25.4 在 QR 的用途
- 给 IC / Rank IC / Sharpe 加置信区间（衔接 §8/§11），回答"这个因子显著吗"比单点估计稳。
- Deflated Sharpe（§11.5）之外，Bootstrap 是判断过拟合的另一把尺。
- 回测绩效的不确定性量化（衔接 §30 回测方法论）。

### 25.5 ⚠️ 注意
- Bootstrap 只能反映**已有样本**的变异，不能创造没见过的 regime（§14.9 结构突变救不了）。
- 若原样本含前视偏差（§20），Bootstrap 只是"精确地把偏差重采样一遍"——垃圾进垃圾出。

---

## 26. Kelly 仓位与风险预算

### 26.1 为什么固定比例不够
§12 给组合权重（基于均值方差），但**单笔下注多少**是另一问题。Kelly 回答：在已知胜率/赔率下，下多大注能**长期对数财富增长最快**且**不破产**。

### 26.2 Kelly 公式（二元赌局）
$$f^*=\frac{bp-q}{b}=\frac{p(b+1)-1}{b}$$
- $p$ 胜率，$q=1-p$ 败率，$b$ 净赔率（赢时赚 b 倍本金）。
- $f^*$ 是最优下注比例（占本金）；$f^*<0$ 表示应反向下注。

### 26.3 连续收益版（QR 更相关）
对序列收益 $r_t$，Kelly 分数 $f^*\approx\frac{\mu}{\sigma^2}$（μ 期望收益、σ² 方差）。
- 直觉：收益越高、波动越低 → 下注越多；但 σ² 在分母，**波动是天然刹车**。
- 变量敏感性：μ↑ → f*↑；σ²↑ → f*↓（剧烈）；μ/σ²→0 或负 → 不下注。

### 26.4 实务：Fractional Kelly（半凯利）
满仓 Kelly 波动极大（回撤可超 50%），实务用 **半凯利 f*/2 或 1/4**，在增长与平稳间折中。
- 衔接 §8 Drawdown：激进 Kelly 的 MDD 常不可接受，故 §12 优化常加杠杆/仓位上限约束。

### 26.5 与风险预算/§12 的关系
- Kelly 是"单资产/单信号"的最优注；组合层 §12.3.1 风险平价（RC 等贡献）是多资产的风险预算，思想同源：让"风险贡献"而非"金额权重"均衡。
- 局限：依赖 μ/σ 估计（§12.6 说 μ 最难估），估计误差下满仓 Kelly 会严重 overbet——这也是为何用 fractional + §10 收缩协方差。

---

## 27. 协整与配对交易

### 27.1 为什么单位根不够
§14.2 讲平稳性：两股票各自非平稳（单位根、随机游走），但**它们的某种线性组合可能平稳**——这就是协整。是配对交易（统计套利）的数学基础。

### 27.2 定义
序列 $X_t,Y_t$ 各自 I(1)（一阶单整、非平稳），若存在 β 使
$$Z_t=Y_t-\beta X_t \quad \text{为平稳 I(0)}$$
则称 X,Y **协整**，β 为协整向量（长期均衡比价）。
- 直觉：两资产价格"长期绑在一起"，短期偏离会**均值回复**（衔接 §14.2 平稳性、§18.2 随机游走的反面）。

### 27.3 检验：Engle-Granger
1. 对 $Y_t=\beta X_t+\varepsilon_t$ 回归得 β（OLS，衔接 §4）。
2. 对残差 $Z_t$ 做 ADF 检验（§14.2）。
3. ADF 拒绝单位根 → 残差平稳 → 协整成立。
- 变量敏感性：样本越长 → ADF 功效越高；结构性断点（§14.9）→ 伪协整（关系已破）。

### 27.4 配对交易策略
- 当 $Z_t$ 偏离其均值 > k·std → 做空贵的、做多便宜的，押注回归。
- 闭合：Z 回到均值平仓。本质是**均值回复 + 市场中性**（衔接 §29）。
- 风险：协整关系断裂（regime change）→ 止损必须硬（§20 数据/结构突变）。

### 27.5 与前面章节闭环
- 衔接 §14 时间序列（单位根/ADF/均值回复）、§18 随机游走（协整是"游走间被锁住"）。
- 衔接 §12 优化（配对是零成本市场中性组合）、§16 VaR（配对回撤有限但断裂时尖峰）。
- ⚠️ 协整是低频慢信号，需配合 §20 防前视（用历史窗口估 β，滚动更新）。

---

## 28. Fama-MacBeth 与两类因子模型

### 28.1 为什么需要区分"两条路线"
§6/§7 讲了"横截面回归估因子收益 f"。但因子研究有**两条根本不同的范式**，混淆会导致看不懂文献/Barra：
- **时序因子模型（Time-Series / Barra 路线）**：个股收益对**已知因子收益序列**时序回归，估的是该股票对因子的**暴露 B(=β)**。
- **横截面因子模型（Cross-Sectional / Fama-MacBeth 路线）**：每期用**横截面回归**估**因子溢价 f**，再跨期检验显著性。

### 28.2 时序路线（估暴露 B）
对股票 i，时序回归：
$$r_{i,t}=\alpha_i+\sum_k B_{i,k}f_{k,t}+\varepsilon_{i,t}$$
- 输入：因子收益 $f_{k,t}$（如市值/价值/动量因子组合收益，事先定义）。
- 输出：**暴露 $B_{i,k}$**（该股票对第 k 因子的敏感度）。
- 这就是 §6 风险模型 $\Sigma=B V B^\top+\Delta$ 里 B 的来源之一（Barra 用此法）。
- 衔接 §6.3：那里是反过来——用截面估 f；时序路线是先有 f 估 B。**方向相反，别搞混**。

### 28.3 Fama-MacBeth（估因子溢价 f）
两阶段：
1. **截面阶段（每期 t）**：用当时暴露 $B_{i,t}$ 对个股收益回归得该期因子溢价 $\hat f_t$：
   $$r_{i,t}=\sum_k B_{i,k,t}f_{k,t}+\eta_{i,t}$$
2. **时序阶段**：把 T 期 $\hat f_t$ 取均值、算 t 值（§11.2）检验"因子溢价是否显著 ≠0"。
- 输出：**因子是否真的有风险溢价**（不是某只股票，而是因子本身）。
- 衔接 §11：第二步的 t 值就是因子显著性判据（衔接 COLLABORATION_PLAN `validate_factors`）。

### 28.4 两条路线怎么选 / 关系
- 时序路线答"**这股票暴露多少**"（组合构建/风险归因用，§12）。
- Fama-MacBeth 答"**这因子值不值得**"（因子筛选/定价用）。
- 两者互补：先用 FMB 证明因子有效，再用时序暴露 B 建组合/估 Σ。
- 共同前提：§20 数据干净、§15 暴露预处理、§14 时序切分。

### 28.5 ⚠️ 注意
- FMB 假设截面独立，实则股票收益相关（违反）→ 标准误低估，需用 Newey-West 或 §25 Bootstrap 修正。
- "因子收益 f"在两条路线角色不同：时序路线里 f 是**已知输入**，FMB 里 f 是**估计输出**——回到 §7 的提醒。

---

## 29. 多空与市场中性组合构建

### 29.1 为什么需要组合构建层
§6/§12 给了因子暴露与优化框架，但**怎么把因子变成可交易头寸**还有一层：多空/市场中性是量化最基础的持仓形态。

### 29.2 多空组合 Long-Short
- 构造：按因子打分排序，做多 Top 组、做空 Bottom 组（等权或按分位）。
- 收益：多空收益 = 高分组合收益 − 低分组合收益，近似**剥离市场风险**（两端 beta 抵消）。
- 变量敏感性：分组数↑→ 每组更纯但样本更少、噪声↑；多空价差越大 → 组合收益越高（因子越强）。
- 衔接 §8 IC：多空组合收益与 IC 正相关（因子排序能力越强，多空越赚）。

### 29.3 市场中性 Market-Neutral
- 目标：组合对**市场因子 beta ≈ 0**，还对行业/风格中性（§15.3 中性化同源）。
- 做法：在 §12.3 优化里加约束 $\sum_i w_i\beta_i=0$、$\sum_i w_i\text{industry}_i=0$。
- 变量敏感性：中性约束↑ → 可交易空间↓、收益↓但波动/回撤更稳定（对冲了大盘风险）。
- 衔接 §12.3.1 风险平价：中性是"对特定因子零暴露"，风险平价是"对各风险等贡献"，可叠加。

### 29.4 杠杆与资金
- 多空常需**净杠杆**（多空各 100% → 总敞口 200%、净 0），放大绝对收益也放大成本（§8.6 交易成本/容量）。
- 变量敏感性：杠杆 L↑ → 收益与回撤同比例↑；成本占比↑（尤其 §20 滑点/冲击在大单时）。

### 29.5 ⚠️ 注意
- 多空≠无风险：中性约束失效（如齐跌时多空齐损）、做空成本（借券/涨停限空）、§27 协整断裂同样适用。
- 实务先 §28 FMB 证明因子有效，再 §12 优化建多空/中性组合，最后 §30 回测。

---

## 30. 系统性回测方法论：怎么回测一个因子

### 30.1 为什么需要专节
前面零散讲了 walk-forward（§14.7）、防前视（§20）、交易成本（§8.6）、显著性（§11）。本节把它们**串成一条可执行的回测流水线**，直接对应 AlphaStream 的 `validate_factors` / `build_factor_exposure`。

### 30.2 标准流水线（7 步）
1. **数据准备（PIT）**：按 §20 取时点数据、含退市宇宙、对齐公司行动。
2. **因子计算**：原始特征 → §15 winsorize →（中性化）→ 标准化 → 得暴露 X/B。
3. **信号生成**：按暴露打分（§5 z-score / §13 模型输出），严格只用 t 时刻信息。
4. **组合构建**：按 §29 多空/中性，或 §12 优化得权重 $w_t$。
5. **成交假设**：用 t+1 开盘/收盘（§20.2），扣 §8.6 交易成本/冲击/容量。
6. **绩效与显著性**：算 §8 指标（Sharpe/IC/IR），用 §11 t 值 / §25 Bootstrap 判显著。
7. **滚动验证**：§14.7 walk-forward 切窗，禁止随机 K-fold（§14.8），看 §17 衰减。

### 30.3 回测的"死亡清单"（任一即作废）
- ☐ 用了未来数据（§20.2）　☐ 宇宙不含退市（§20.3）　☐ 非 PIT（§20.4）
- ☐ 当天价成交　☐ 忽略成本（§8.6）　☐ 随机切分混时空（§14.8）
- ☐ 样本内调参后报样本内（须 OOS，§11/§17）　☐ 单一历史片段（须多 regime，§14.9）

### 30.4 与全文档闭环
- 数据层 §20 → 因子层 §15/§6 → 模型层 §28/§13 → 组合层 §12/§29 → 风险层 §10/§16/§22/§23 → 验证层 §11/§25/§14 → 监控层 §17。
- 一份合格回测 = 上述每层都过关；任一层的坑都会在样本外（§17）兑现。

### 30.5 ⚠️ 注意
- 回测是**对假设的检验**，不是对未来的承诺（§17 衰减 + §14.9 结构突变）。
- 过度优化（§11.4 p-hacking）会让回测"完美"而实盘崩溃——walk-forward + DSR（§11.5）+ 简约因子（经济含义）是三道护栏。

---

### 本文档范围说明
这是**持续扩充的基础知识库**，现已覆盖 **§1–§30**：线性代数 → 方差/协方差/相关性 → 协方差矩阵 Σ → 回归 → z-score → 多因子模型串联（§1–§7）→ 金融绩效指标（§8）→ 特征值/特征向量与 PCA（§9）→ 收缩估计 Ledoit-Wolf（§10）→ 假设检验（§11）→ 最优化 Markowitz（§12）→ 常用机器学习方法（§13）→ 时间序列（§14）→ 因子预处理 winsorize/中性化/正交化（§15）→ 风险度量 VaR/CVaR（§16）→ 因子衰减（§17）→ 随机过程（§18）→ 对数收益 vs 简单收益（§19）→ 数据三大陷阱 前视/幸存者/PIT（§20）→ 对照术语表补遗（§21）→ 偏度/峰度高阶矩（§22）→ 肥尾/Student-t/Cornish-Fisher VaR（§23）→ EWMA 指数加权协方差（§24）→ Bootstrap 置信区间（§25）→ Kelly 仓位与风险预算（§26）→ 协整与配对交易（§27）→ Fama-MacBeth 与两类因子模型（§28）→ 多空/市场中性组合构建（§29）→ 系统性回测方法论（§30）。
至此，量化研究（多因子 + 最小方差组合）所需的基础数学/统计/流程知识已系统闭环。后续如需更深入（Black-Litterman、随机波动模型、深度学习因子、高频微观结构等），可在对应章节延伸。
